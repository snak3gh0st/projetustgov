---
phase: debug-session-2026-04-08
status: completed
last_updated: 2026-04-08T20:27:15.727Z
---

<current_state>
Sessão de bugfixes concluída. Todos os 6 commits pushed para origin/master.
Nenhum trabalho em andamento — próxima sessão pode atacar os debug sessions ainda abertos.
</current_state>

<completed_work>

- ✅ fix: lumiart situação desatualizada na TGov Execução
  - Root cause: tabela `propostas` não tinha sync automático de `situacao`
  - Fix: STEP B2 em `execucao-sync.ts` — UPDATE batch via unnest com double NOT EXISTS guard
  - Confirmado: 7534 propostas atualizadas, lumiart verified `updated_at: 2026-04-08 20:23`

- ✅ fix: aprovação coberta pelo mesmo STEP B2 (mesmo branch 3 do CTE)

- ✅ fix: repo-sync STEP 8 log enganoso ("no pending CNPJs" quando era timeout)
  - `elapsed` capturado antes das ops 8a–8e; agora `elapsedStep8` capturado após fetch da queue

- ✅ perf: repo-sync STEP 7 de 225s → 86s
  - 3564 queries individuais → 1 batch unnest query
  - STEP 8 agora executa (enrichment de 29 CNPJs no primeiro run)

- ✅ fix: dedup leads por conflict key antes do batch
  - 148 duplicatas detectadas e removidas silenciosamente

- ✅ feat: botão Sincronizar dispara sync-leads + sync-execucao em paralelo
  - `Promise.allSettled` — falha do execucao não bloqueia leads
  - Status bar mostra stats de ambos
</completed_work>

<remaining_work>
Debug sessions ainda abertas (não investigadas esta sessão):
- `.planning/debug/admin-csv-export-forbidden.md`
- `.planning/debug/comissoes-wellington-nao-aparecem.md`
- `.planning/debug/commission-sales-flow.md`
- `.planning/debug/contacted-status-regression.md`
- `.planning/debug/duplicate-lead-cnpj.md`
- `.planning/debug/vendas-nao-aparecem-crm-vitoria-gabriel.md`
</remaining_work>

<decisions_made>

- STEP B2 usa NOT EXISTS duplo (projetos_execucao + tgov_projetos_execucao) — espelha exatamente o branch 3 do CTE all_exec, zero cross-contamination
- Guard `propostas.nr_proposta IS NOT NULL` adicionado para evitar bypass das guards quando nr_proposta é NULL
- Índice `ix_tgov_pe_nr_proposta` adicionado em `tgov-tables.ts` para o NOT EXISTS ser eficiente
- Batch unnest com `dedupMap` por conflict key `cnpj|codigo_programa|nr_emenda` — last-write-wins preserva comportamento anterior do row-by-row
- `Promise.allSettled` no botão sync — independência entre os dois crons
</decisions_made>

<blockers>
Nenhum.
</blockers>

<context>
Sessão iniciou com report do lumiart na TGov Execução com situação desatualizada.
Debug revelou que tabela `propostas` não tinha nenhum cron atualizando `situacao` — só um script manual one-time.
Fix STEP B2 usa o propostaMap já construído no STEP B (siconv_proposta.csv já baixado) — zero custo adicional de I/O.
Durante verificação, descobrimos repo-sync a 225s bloqueando STEP 8. Root cause: 3564 queries individuais no STEP 7. Batch unnest resolveu.
</context>

<next_action>
Atacar os debug sessions abertos, começando pelo mais crítico para o negócio.
Ou continuar fase 20 (TGov sidecard UI) — ver `.planning/phases/20-tgov-ajustes-0704/`.
</next_action>
