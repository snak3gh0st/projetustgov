---
phase: quick
plan: 260322
type: execute
wave: 1
depends_on: []
files_modified:
  - web/scripts/verify-execucao-tags-rendimento.mjs
  - web/src/app/api/execucao/route.ts
  - web/src/app/execucao/ExecucaoClient.tsx
autonomous: true
requirements: [EXEC-TAGS-RENDIMENTO]

must_haves:
  truths:
    - "Leads com exatamente 5 propostas deixam de ficar sem classificacao de maturidade e passam a aparecer como Autossuficiente"
    - "A tag Rendimento so aparece quando existe rendimento acima de R$ 5.000 e saldo em conta maior ou igual ao rendimento no mesmo convenio"
    - "Leads com rendimento baixo ou ja consumido nao recebem a tag Rendimento"
  artifacts:
    - path: "web/src/app/api/execucao/route.ts"
      provides: "Regras SQL consolidadas para tag_autossuficiente, tag_iniciante e tag_rendimento no GET /api/execucao"
      contains: "tag_rendimento"
    - path: "web/src/app/execucao/ExecucaoClient.tsx"
      provides: "Texto/tooltip da UI alinhado com as novas regras de Autossuficiente e Rendimento"
      contains: "Rendimento"
    - path: "web/scripts/verify-execucao-tags-rendimento.mjs"
      provides: "Verificacao automatizada de regressao para limiar de 5 propostas e regra de rendimento"
  key_links:
    - from: "web/src/app/api/execucao/route.ts"
      to: "projetos_execucao"
      via: "BOOL_OR e comparacoes por convenio com rendimento_aplicacao e saldo_conta"
      pattern: "rendimento_aplicacao|saldo_conta|tag_rendimento"
    - from: "web/src/app/api/execucao/route.ts"
      to: "proposta_counts"
      via: "classificacao por total_propostas_db"
      pattern: "COALESCE\(pc.cnt, 0\) >= 5|COALESCE\(pc.cnt, 0\) < 5"
---

<objective>
Corrigir a regra de classificacao da TAG RENDIMENTO e o limiar da tag de maturidade no Lead Execucao.

Purpose: eliminar falsos positivos de Rendimento e remover o estado indefinido dos CNPJs com 5 propostas, mantendo a classificacao coerente com a leitura comercial pedida pelo usuario.
Output: GET `/api/execucao` com regras revisadas, UI com texto coerente com a nova regra e um script automatizado de regressao para validar os dois cenarios.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260320-dj7-add-execution-tags-autossuficiente-5-pro/260320-dj7-SUMMARY.md
@web/src/app/api/execucao/route.ts
@web/src/app/execucao/ExecucaoClient.tsx

<interfaces>
From web/src/app/api/execucao/route.ts:
```ts
interface ExecucaoAggRow {
  total_saldo: string
  total_propostas_db: number
  tag_autossuficiente: boolean
  tag_iniciante: boolean
  tag_rendimento: boolean
}
```

Current grouped SQL outputs:
```sql
COALESCE(pc.cnt, 0) > 5 AS tag_autossuficiente,
COALESCE(pc.cnt, 0) < 5 AS tag_iniciante,
rc.cnpj IS NOT NULL AS tag_rendimento
```

From web/src/app/execucao/ExecucaoClient.tsx:
```tsx
title="Mais de 5 propostas executadas"
title="Rendimento bancario significativo"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar verificacao automatizada de regressao para tag Rendimento e limiar de 5 propostas</name>
  <files>web/scripts/verify-execucao-tags-rendimento.mjs</files>
  <action>
Crie um script Node ESM focado em regressao de negocio para `/api/execucao`, usando `pg` e `DATABASE_URL || POSTGRES_URL` como os outros scripts do projeto.

O script deve executar uma query de verificacao e falhar com `process.exit(1)` se qualquer uma destas condicoes for violada:
1. existe algum CNPJ com `total_propostas_db = 5` que nao saiu como `tag_autossuficiente = true` e `tag_iniciante = false`;
2. existe algum CNPJ marcado com `tag_rendimento = true` sem ter ao menos um convenio em `projetos_execucao` com `rendimento_aplicacao > 5000` e `saldo_conta >= rendimento_aplicacao`;
3. existe algum CNPJ sem nenhum convenio elegivel pela regra acima que ainda assim ficou com `tag_rendimento = true`.

Imprima um resumo curto com contagens verificadas e exemplos de falha quando houver. Nao transforme este script em teste generico do modulo execucao; ele deve ser pequeno e dedicado a esta quick task.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && node scripts/verify-execucao-tags-rendimento.mjs</automated>
  </verify>
  <done>Existe um comando automatizado que quebra quando o limiar de 5 propostas ou a regra de Rendimento fogem do comportamento esperado.</done>
</task>

<task type="auto">
  <name>Task 2: Revisar a regra SQL da tag Rendimento e remover o estado indefinido dos CNPJs com 5 propostas</name>
  <files>web/src/app/api/execucao/route.ts</files>
  <action>
Atualize apenas a regra agregada do GET `/api/execucao`.

Implementacao esperada:
1. Troque o limiar de maturidade para que `tag_autossuficiente` seja `COALESCE(pc.cnt, 0) >= 5`.
2. Mantenha `tag_iniciante` como `COALESCE(pc.cnt, 0) < 5`, garantindo que CNPJ com exatamente 5 propostas nao fique sem tag e tambem nao apareca como iniciante.
3. Substitua a logica atual de `tag_rendimento` baseada no CTE `rendimento_cnpjs` por uma regra ligada a `projetos_execucao`: a tag so deve ser verdadeira quando existir pelo menos um convenio do mesmo CNPJ com `rendimento_aplicacao > 5000` e `saldo_conta >= rendimento_aplicacao`.
4. Modele a regra com agregacao SQL no mesmo fluxo do `agg` (preferencialmente `BOOL_OR(pe.rendimento_aplicacao > 5000 AND COALESCE(pe.saldo_conta, 0) >= pe.rendimento_aplicacao)`) para manter a leitura por convenio e evitar depender da tabela antiga `convenios`.
5. Remova o CTE/join que ficou obsoleto depois da troca de regra. Nao altere filtros, ordenacao, status CRM, contatos, slide-over nem outras tags.

Se a implementacao sugerida revelar necessidade de tratar `NULL`, normalize com `COALESCE(..., 0)` sem mudar o significado da regra de negocio.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && node scripts/verify-execucao-tags-rendimento.mjs && npx tsc --noEmit --pretty false</automated>
  </verify>
  <done>`/api/execucao` passa a classificar CNPJs com 5 propostas como Autossuficiente e so marca Rendimento quando houver rendimento acima de R$ 5 mil ainda coberto pelo saldo do convenio.</done>
</task>

<task type="auto">
  <name>Task 3: Alinhar os textos da UI com as novas regras de classificacao</name>
  <files>web/src/app/execucao/ExecucaoClient.tsx</files>
  <action>
Ajuste somente os textos de apoio da UI que ficaram defasados com a mudanca de regra:
1. atualize o tooltip de `Autossuficiente` para refletir `5 ou mais propostas`;
2. atualize o tooltip de `Rendimento` para refletir explicitamente `rendimento acima de R$ 5 mil com saldo compativel / nao consumido`;
3. nao mude cores, nomes das tags, filtros, ordenacao, nem o layout da tabela.

Esta task existe para evitar que a tela continue explicando a regra antiga depois da correcao SQL.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && npx tsc --noEmit --pretty false</automated>
  </verify>
  <done>Os tooltips da coluna Tags descrevem corretamente as novas regras de Autossuficiente e Rendimento sem alterar o restante da interface.</done>
</task>

</tasks>

<verification>
- `cd web && node scripts/verify-execucao-tags-rendimento.mjs` passa
- `cd web && npx tsc --noEmit --pretty false` passa
- Nenhum CNPJ com exatamente 5 propostas fica sem classificacao de maturidade
- A tag Rendimento deixa de depender da tabela `convenios` antiga e passa a refletir rendimento > R$ 5 mil ainda coberto pelo saldo
</verification>

<success_criteria>
- A classificacao de maturidade nao tem mais buraco no caso `5 propostas`
- A tag Rendimento representa apenas saldo com rendimento relevante e ainda nao consumido
- Existe verificacao automatizada para impedir regressao futura dessas duas regras
</success_criteria>

<output>
After completion, create `.planning/quick/260322-atualizar-o-vinculo-da-tag-rendimento-pa/260322-SUMMARY.md`
</output>
