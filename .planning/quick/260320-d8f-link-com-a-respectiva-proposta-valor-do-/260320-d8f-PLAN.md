---
phase: quick
plan: 260320-d8f
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/api/execucao/route.ts
  - web/src/app/execucao/ExecucaoClient.tsx
  - web/src/components/ExecucaoSlideOver.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "List table shows total Valor do Convenio (SUM valor_global) per CNPJ"
    - "List table Propostas column uses inverted color scheme — red/warm tones for high counts (low priority) and neutral for low counts"
    - "Slide-over shows total Valor do Convenio as a summary header above individual convenio cards"
  artifacts:
    - path: "web/src/app/api/execucao/route.ts"
      provides: "total_valor_global field in grouped response"
      contains: "total_valor_global"
    - path: "web/src/app/execucao/ExecucaoClient.tsx"
      provides: "Valor Convenio column + improved propostas priority coloring"
      contains: "total_valor_global"
    - path: "web/src/components/ExecucaoSlideOver.tsx"
      provides: "Summary header with total valor_global"
      contains: "totalValorGlobal"
  key_links:
    - from: "web/src/app/execucao/ExecucaoClient.tsx"
      to: "/api/execucao"
      via: "fetch response includes total_valor_global"
      pattern: "total_valor_global"
    - from: "web/src/app/execucao/ExecucaoClient.tsx"
      to: "web/src/components/ExecucaoSlideOver.tsx"
      via: "passes totalValorGlobal prop from aggregated row"
      pattern: "totalValorGlobal"
---

<objective>
Add Valor do Convenio to the execucao list and slide-over, and improve the Propostas priority indicator coloring.

Purpose: The list view is missing the total convenio value (valor_global) per CNPJ — only desembolsado and saldo are shown. The slide-over shows valor_global per-convenio but lacks a summary total. Also, the Propostas column color scheme (green for 6+) misleadingly suggests high-count leads are "good" when they should be flagged as low priority (more past projects = entity already well-served).

Output: Updated API, list table with Valor Convenio column, improved propostas color logic, slide-over summary header.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@web/src/app/api/execucao/route.ts
@web/src/app/execucao/ExecucaoClient.tsx
@web/src/components/ExecucaoSlideOver.tsx
@web/src/lib/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add total_valor_global to API and list table, fix propostas priority colors</name>
  <files>web/src/app/api/execucao/route.ts, web/src/app/execucao/ExecucaoClient.tsx</files>
  <action>
**API (route.ts):**
1. Add `SUM(pe.valor_global) AS total_valor_global` to the SELECT in the GROUP BY query (after the existing `total_saldo` line).
2. Add `total_valor_global: string` to the `ExecucaoAggRow` interface.

**ExecucaoClient.tsx:**
1. Add `total_valor_global: string` to the `ExecucaoAggRow` interface.
2. Add a new column header `{ key: 'valor_convenio', label: 'Valor Convenio' }` — insert it after `'uf'` and before `'fomentos'` in the table headers array.
3. Add the corresponding `<td>` in the row rendering: `formatCompactCurrency(row.total_valor_global)` — use `text-sm font-bold text-gray-900` styling to make it prominent.
4. Add sorting case: `case 'valor_convenio': va = Number(a.total_valor_global); vb = Number(b.total_valor_global); break`
5. Add `total_valor_global` to the KPI row: replace the existing "Total Fomentos" KPI or add a new KPI "Valor Total Convenios" with `formatCompactCurrency(rows.reduce((s, r) => s + Number(r.total_valor_global), 0))` and icon `\u{1F4B0}`. Keep existing KPIs — add as 5th item if needed, or replace "Total Fomentos" which is less useful.

**Fix Propostas column colors (ExecucaoClient.tsx):**
Invert the color scheme to communicate "more = lower priority":
- `total_propostas_db >= 6` -> `bg-red-50 text-red-700 border border-red-200` (many projects = lowest priority, needs attention flag)
- `total_propostas_db >= 3` -> `bg-orange-50 text-orange-700 border border-orange-200` (moderate = medium-low priority)
- `total_propostas_db >= 1` -> `bg-gray-50 text-gray-600 border border-gray-200` (few = neutral)
Update the title text to: `${row.total_propostas_db} proposta(s) ja executadas — quanto mais, menor a prioridade`
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /execucao page — confirm new "Valor Convenio" column appears, propostas badges use warm colors for high counts</manual>
  </verify>
  <done>List table shows Valor Convenio column with formatted currency values. Propostas column uses red for 6+, orange for 3-5, gray for 1-2. Sorting works on the new column.</done>
</task>

<task type="auto">
  <name>Task 2: Add total Valor Convenio summary to slide-over header</name>
  <files>web/src/components/ExecucaoSlideOver.tsx, web/src/app/execucao/ExecucaoClient.tsx</files>
  <action>
**ExecucaoSlideOver.tsx:**
1. Add `totalValorGlobal: string | null` and `totalPropostas: number` to the `ExecucaoSlideOverProps` interface.
2. In the header section (after the alert/contact badges div, around line 119), add a summary row showing:
   - Total Valor Convenio: `formatCompactCurrency(totalValorGlobal)` in a prominent `text-lg font-bold text-gray-900` style with label "Valor total dos convenios" in `text-xs text-gray-500`.
   - If `totalPropostas > 0`, show a small inline badge: `{totalPropostas} proposta(s) ja executada(s)` with the same warm-color scheme from Task 1 (red for 6+, orange for 3-5, gray for 1-2).
3. Layout: Use a `mt-4 pt-4 border-t border-gray-200` div to separate from header badges. Inside, a flex row with valor on the left and propostas badge on the right.

**ExecucaoClient.tsx:**
1. Pass the new props to `<ExecucaoSlideOver>`:
   - `totalValorGlobal={selectedRow?.total_valor_global ?? null}`
   - `totalPropostas={selectedRow?.total_propostas_db ?? 0}`
2. Extract `selectedRow` as: `const selectedRow = rows.find(r => r.cnpj === selectedCnpj)` — compute once instead of calling `.find()` multiple times (currently done 3 times for nomeProponente, temAlerta, contactPresent). Use `selectedRow` for all existing props too.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Click a CNPJ row in /execucao — slide-over header shows total valor convenio and propostas badge</manual>
  </verify>
  <done>Slide-over header displays total Valor Convenio and propostas count badge. ExecucaoClient uses single selectedRow lookup instead of 3 separate find() calls.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- /execucao list table shows "Valor Convenio" column between UF and Fomentos
- Propostas badges: red for 6+, orange for 3-5, gray for 1-2
- Slide-over header shows total valor and propostas summary
- All existing sorting, filtering, and slide-over functionality unchanged
</verification>

<success_criteria>
- Total valor_global (SUM) visible in both the list table and slide-over header
- Propostas color scheme clearly communicates "more = lower priority" using warm/red tones
- No TypeScript compilation errors
- No regressions in existing execucao functionality
</success_criteria>

<output>
After completion, create `.planning/quick/260320-d8f-link-com-a-respectiva-proposta-valor-do-/260320-d8f-SUMMARY.md`
</output>
