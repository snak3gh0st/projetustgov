---
phase: quick
plan: 260323
type: execute
wave: 1
depends_on: []
files_modified:
  - web/scripts/verify-dashboard-execucao-pipeline.mjs
  - web/src/app/api/dashboard-crm/route.ts
  - web/src/app/page.tsx
autonomous: true
requirements: [PIPE-EXEC-01]

must_haves:
  truths:
    - "A guia atual do Pipeline continua existindo, mas passa a mostrar o pipeline de aprovacao separado do pipeline de execucao"
    - "O pipeline de aprovacao continua usando a base atual de vendedor_projetos sem misturar os CNPJs de execucao na mesma contagem visual"
    - "O pipeline de execucao conta apenas CNPJs presentes em projetos_execucao e mostra a etapa comercial de cada CNPJ em um bloco proprio"
    - "Gestor/coordenador conseguem enxergar os dois funis no mesmo dashboard sem precisar alternar de rota para comparar aprovacao vs execucao"
  artifacts:
    - path: "web/src/app/api/dashboard-crm/route.ts"
      provides: "Payload do dashboard com um bloco dedicado de contagens para pipeline de execucao"
      contains: "execucao_pipeline"
    - path: "web/src/app/page.tsx"
      provides: "Renderizacao separada de Pipeline Aprovacao e Pipeline Execucao na home"
      contains: "Pipeline Execucao"
    - path: "web/scripts/verify-dashboard-execucao-pipeline.mjs"
      provides: "Verificacao automatizada das contagens do pipeline de execucao"
  key_links:
    - from: "web/src/app/api/dashboard-crm/route.ts"
      to: "projetos_execucao"
      via: "agregacao por CNPJ com status CRM priorizado"
      pattern: "execucao_pipeline|projetos_execucao"
    - from: "web/src/app/page.tsx"
      to: "/api/dashboard-crm"
      via: "DashboardData com bloco separado para execucao"
      pattern: "execucao_pipeline"
---

<objective>
Criar um pipeline de execucao dentro da mesma guia atual do Pipeline, mas separado visual e semanticamente do pipeline de aprovacao.

Purpose: permitir comparacao rapida entre os dois universos comerciais sem misturar contagens nem obrigar o gestor a trocar de tela para entender onde cada CNPJ esta.
Output: dashboard API com agregacao dedicada para execucao, home page com dois blocos de pipeline e um verificador automatizado da nova contagem.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260321-coluna-de-status-no-lead-execucao-com-os/260321-SUMMARY.md
@.planning/quick/51-hot-fix-ambiente-tito-para-vendedor-sem-/51-SUMMARY.md
@web/src/app/api/dashboard-crm/route.ts
@web/src/app/api/execucao/route.ts
@web/src/app/page.tsx

<interfaces>
From web/src/app/page.tsx:
```ts
interface StatusCounts {
  'Não Contatado': number
  'Retorno': number
  'Proposta': number
  'Aguardando Closer'?: number
  'Fechado': number
  [key: string]: number | undefined
}

interface DashboardData {
  role?: string
  global: GlobalStats
  vendedores: VendedorStats[]
}

const STATUS_ORDER = ['Não Contatado', 'Ainda Não', 'Retorno', 'Proposta', 'Aguardando Closer', 'Fechado'] as const
```

From web/src/app/api/execucao/route.ts:
```sql
SELECT CASE
  WHEN COALESCE(vp.status_contato, 'Não Contatado') IN ('Nao Contatado', 'Não Contatado') THEN 'Não Contatado'
  ELSE COALESCE(vp.status_contato, 'Não Contatado')
END AS crm_status
```

Execution status palette already used in /execucao:
```ts
const STATUS_COLORS = {
  'Não Contatado': 'bg-orange-50 text-orange-600',
  'Ainda Não': 'bg-yellow-50 text-yellow-600',
  'Retorno': 'bg-amber-50 text-amber-600',
  'Quente': 'bg-red-50 text-red-600',
  'Muito Quente': 'bg-red-100 text-red-700',
  'Proposta': 'bg-blue-50 text-[#0072F7]',
  'Aguardando Closer': 'bg-purple-50 text-purple-600',
  'Fechado': 'bg-green-50 text-green-600',
  'Telefone Invalido': 'bg-gray-50 text-gray-500',
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar verificador automatizado das contagens do pipeline de execucao</name>
  <files>web/scripts/verify-dashboard-execucao-pipeline.mjs</files>
  <action>
Crie um script Node ESM pequeno e dedicado a esta quick task usando `pg` com `DATABASE_URL || POSTGRES_URL`.

O script deve consultar o banco e falhar com `process.exit(1)` se qualquer uma destas regras quebrar:
1. o universo de execucao nao estiver sendo contado por `COUNT(DISTINCT cnpj)` sobre `projetos_execucao`;
2. existir CNPJ em `projetos_execucao` sem cair em nenhuma bucket de status do pipeline de execucao;
3. a soma das buckets devolvidas pela mesma logica SQL planejada para o dashboard nao bater com o total distinto de CNPJs em execucao.

Modele a bucketizacao com a mesma normalizacao/prioridade que sera usada no dashboard, reaproveitando a leitura comercial atual de `crm_status`. Imprima total de CNPJs, total por status e exemplos de divergencia quando houver. Nao transforme isso em teste amplo do dashboard inteiro; ele existe apenas para blindar a separacao do funil de execucao.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && node scripts/verify-dashboard-execucao-pipeline.mjs</automated>
  </verify>
  <done>Existe um comando automatizado que acusa imediatamente se o novo pipeline de execucao perder CNPJs, duplicar contagem ou deixar status fora do funil.</done>
</task>

<task type="auto">
  <name>Task 2: Expor um bloco execucao_pipeline separado no dashboard CRM</name>
  <files>web/src/app/api/dashboard-crm/route.ts</files>
  <action>
Adicione uma nova agregacao paralela ao GET `/api/dashboard-crm` para devolver um bloco `execucao_pipeline` separado do `global.by_status` atual.

Implementacao esperada:
1. Conte somente CNPJs presentes em `projetos_execucao`, sempre com `COUNT(DISTINCT ...)` para nao inflar linhas com varios convenios.
2. Derive um unico status comercial por CNPJ usando a mesma prioridade de CRM ja usada em `/api/execucao` (Fechado > Aguardando Closer > Proposta > Retorno > Ainda Não > Quente > Muito Quente > Telefone Invalido > Não Contatado), normalizando `Nao Contatado` para `Não Contatado`.
3. Retorne `execucao_pipeline` no JSON com formato enxuto: `{ total_cnpjs, by_status }`.
4. Inclua no `by_status` todas as chaves que a UI vai renderizar no bloco de execucao, mesmo quando zeradas, para evitar condicionais espalhadas no client.
5. Nao altere a semantica do bloco `global` existente: o pipeline de aprovacao continua vindo de `vendedor_projetos` como hoje. A separacao entre aprovacao e execucao deve acontecer no payload, nao por gambiarra visual no client.

Se precisar escolher entre criar mais uma rota ou estender a rota atual, use a rota atual por ser a opcao mais curta e por manter os dois funis sincronizados num unico fetch da home.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && node scripts/verify-dashboard-execucao-pipeline.mjs && npx tsc --noEmit --pretty false</automated>
  </verify>
  <done>O dashboard passa a devolver um bloco `execucao_pipeline` isolado, com total e buckets por status baseados apenas nos CNPJs de `projetos_execucao`.</done>
</task>

<task type="auto">
  <name>Task 3: Separar visualmente Pipeline Aprovação e Pipeline Execução na home</name>
  <files>web/src/app/page.tsx</files>
  <action>
Atualize a home (`/`) para consumir `execucao_pipeline` e renderizar dois blocos distintos no mesmo tab:
1. renomeie o bloco atual para algo explicito como `Pipeline Aprovação`, mantendo cards, percentuais, grid e links atuais para `/leads?status_contato=...`;
2. adicione logo abaixo um bloco `Pipeline Execução`, com cards proprios e denominador baseado em `execucao_pipeline.total_cnpjs`;
3. use um status order proprio para execucao, compatível com o conjunto retornado pela API; se houver status extras do CRM (ex.: Quente, Muito Quente, Telefone Invalido), renderize-os apenas no bloco de execucao em vez de encaixa-los no funil de aprovacao;
4. mantenha a execucao dentro da mesma tela do pipeline, mas com titulo/subtitulo e espacamento que deixem claro que sao universos diferentes;
5. os cards do bloco de execucao podem navegar para `/execucao` sem filtro nesta quick task. Nao abrir nova rota, nao mexer na sidebar e nao adicionar plumbing de query param em `/execucao` agora — o objetivo aqui e separar visao, nao expandir navegacao.

Reaproveite o maximo possivel do estilo existente (mesma familia de cards, barras e percentuais) para reduzir diff e manter consistencia visual.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && node scripts/verify-dashboard-execucao-pipeline.mjs && npx tsc --noEmit --pretty false</automated>
  </verify>
  <done>A pagina inicial mostra dois funis claramente separados — aprovacao e execucao — dentro da mesma guia de Pipeline, sem misturar contagens ou comportamento de clique.</done>
</task>

</tasks>

<verification>
- `cd web && node scripts/verify-dashboard-execucao-pipeline.mjs` passa
- `cd web && npx tsc --noEmit --pretty false` passa
- `/` mostra um bloco `Pipeline Aprovação` e outro `Pipeline Execução`
- O bloco de aprovação continua apontando para `/leads`, enquanto o bloco de execução abre `/execucao`
</verification>

<success_criteria>
- O gestor enxerga aprovacao e execucao separadamente na mesma guia do pipeline
- As contagens de execucao saem exclusivamente de `projetos_execucao` agrupado por CNPJ
- O pipeline de aprovacao continua intacto e sem regressao semantica
- Existe verificacao automatizada para impedir perda/mistura de CNPJs no novo funil de execucao
</success_criteria>

<output>
After completion, create `.planning/quick/260323-criar-pipeline-de-execucao-podendo-ficar/260323-SUMMARY.md`
</output>
