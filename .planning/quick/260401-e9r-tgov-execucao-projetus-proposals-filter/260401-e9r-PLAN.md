---
phase: quick
plan: 260401-e9r
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/tgov.ts
  - web/src/app/api/tgov/aprovacao/route.ts
  - web/src/app/api/tgov/execucao/route.ts
autonomous: true
requirements: [TGOV-PROJETUS-FILTER]
must_haves:
  truths:
    - "TGov aprovacao tab shows ONLY the ~250 Projetus proposals from the hardcoded whitelist"
    - "TGov execucao tab shows ONLY convenios whose id_proposta matches a Projetus proposal"
    - "The whitelist is defined once in tgov.ts and consumed by both API routes"
  artifacts:
    - path: "web/src/lib/tgov.ts"
      provides: "PROJETUS_PROPOSAL_IDS Set with all ~250 proposal number strings"
      contains: "PROJETUS_PROPOSAL_IDS"
    - path: "web/src/app/api/tgov/aprovacao/route.ts"
      provides: "WHERE clause filtering propostas by transfer_gov_id IN whitelist"
    - path: "web/src/app/api/tgov/execucao/route.ts"
      provides: "WHERE clause filtering projetos_execucao by id_proposta IN whitelist"
  key_links:
    - from: "web/src/app/api/tgov/aprovacao/route.ts"
      to: "web/src/lib/tgov.ts"
      via: "import PROJETUS_PROPOSAL_IDS"
      pattern: "PROJETUS_PROPOSAL_IDS"
    - from: "web/src/app/api/tgov/execucao/route.ts"
      to: "web/src/lib/tgov.ts"
      via: "import PROJETUS_PROPOSAL_IDS"
      pattern: "PROJETUS_PROPOSAL_IDS"
---

<objective>
Filter the TGov dashboard (both aprovacao and execucao tabs) to show ONLY the specific Projetus proposals provided by the client.

Purpose: Currently the TGov tabs show all proposals/convenios (or filter by CNPJ). The client provided a definitive whitelist of ~250 proposal numbers that constitute "Projetus proposals." Both tabs must be scoped to this exact list.

Output: Updated tgov.ts with the whitelist constant, and both API routes filtering by it.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/src/lib/tgov.ts
@web/src/app/api/tgov/aprovacao/route.ts
@web/src/app/api/tgov/execucao/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROJETUS_PROPOSAL_IDS whitelist to tgov.ts</name>
  <files>web/src/lib/tgov.ts</files>
  <action>
Add a new exported constant `PROJETUS_PROPOSAL_IDS` to web/src/lib/tgov.ts containing a `Set<string>` of all Projetus proposal numbers (the number part only, as strings). The values are the number portion from the user's "NUMERO/ANO" list — strip the "/YEAR" suffix since `transfer_gov_id` and `id_proposta` store just the numeric ID.

The full list (extract just the number before the slash):
4822, 4976, 1761, 19819, 4903, 4974, 49113, 23645, 41044, 23611, 23571, 24429, 20231, 23416, 23495, 24318, 41048, 41550, 22834, 23138, 24611, 23387, 23379, 27192, 5311, 9970, 1233, 10129, 5504, 4833, 3343, 5665, 2833, 6203, 22162, 5815, 21604, 2851, 9646, 3566, 5461, 9647, 2589, 9764, 2165, 21990, 21946, 2496, 10113, 2260, 19809, 10315, 3830, 10278, 5471, 9769, 2284, 7459, 9645, 4371, 5422, 5159, 5699, 10351, 9975, 3962, 26464, 5436, 5803, 5516, 10429, 52522, 28342, 52782, 52788, 11167, 10468, 9264, 24542, 62106, 49036, 9136, 9219, 11225, 11240, 60409, 53078, 24119, 9232, 62127, 11027, 11272, 10485, 9207, 61641, 10014, 10648, 10332, 11231, 52822, 24099, 10417, 52762, 38716, 61688, 9672, 64489, 9224, 10384, 23686, 70983, 31060, 32157, 9535, 10326, 8327, 27174, 9976, 48834, 9277, 48537, 20841, 8983, 9479, 23688, 10704, 61470, 46165, 8208, 27191, 11749, 10602, 10959, 48448, 10345, 10638, 10993, 9504, 10617, 61653, 52455, 10539, 6648, 7017, 22969, 5587, 581, 714, 6559, 1767, 2250, 1639, 5158, 5141, 5602, 5066, 1898, 6978, 22059, 1860, 5636, 6448, 35497, 5006, 5181, 22256, 22664, 5675, 21012, 2654, 6933, 621, 6503, 1169, 5646, 5863, 2059, 866, 5115, 5010, 755, 6851, 21473, 1730, 36095, 7070, 35946, 5649, 5520, 1116, 5648, 4795, 22005, 6932, 1797, 6250, 1117, 6905, 16400, 21466, 1858, 6184, 22393, 27516, 45260, 25260, 60105, 5644, 25286, 62534, 20778, 30065, 24745, 24174, 46243, 60672, 25353, 27359, 27357, 25207, 60704, 25279, 24291, 26506, 28488, 62021, 26031, 27265, 30083, 20689, 62169, 60099, 30031, 27206, 60518, 32481, 60108, 30221, 27398, 27403, 27407, 46240, 27221, 26478, 3514, 5373

Note: 11225 appears twice in the original list — deduplicate (Set handles this automatically).

Place the constant AFTER the existing `TGOV_STATUS_ORDER` block and BEFORE `tgovStatusSortKey`. Add a JSDoc comment explaining this is the definitive Projetus proposal whitelist.

Also export a helper function `buildProjetusProposalWhereClause(columnName: string, params: unknown[]): string` that:
1. Adds all proposal IDs to the params array
2. Returns a SQL fragment like `{columnName} IN ($N, $N+1, ..., $N+count)` using parameterized placeholders
3. This avoids SQL injection and keeps the parameterized query pattern used throughout the codebase
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov && npx tsc --noEmit --project web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>PROJETUS_PROPOSAL_IDS Set exported from tgov.ts with all ~250 unique proposal IDs, plus buildProjetusProposalWhereClause helper function</done>
</task>

<task type="auto">
  <name>Task 2: Filter both TGov API routes by Projetus proposal whitelist</name>
  <files>web/src/app/api/tgov/aprovacao/route.ts, web/src/app/api/tgov/execucao/route.ts</files>
  <action>
**aprovacao/route.ts:**
1. Import `buildProjetusProposalWhereClause` from `@/lib/tgov`
2. After initializing `mainParams` and `mainConditions` arrays (line ~30), IMMEDIATELY add the Projetus filter as the FIRST condition by calling `buildProjetusProposalWhereClause('p.transfer_gov_id', mainParams)` and pushing the returned clause into `mainConditions`
3. This ensures ALL queries (total, byStatus, table) are scoped to Projetus proposals only

**execucao/route.ts:**
1. Import `buildProjetusProposalWhereClause` from `@/lib/tgov`
2. After initializing `mainParams` and `mainConditions` arrays (line ~39), IMMEDIATELY add the Projetus filter as the FIRST condition by calling `buildProjetusProposalWhereClause('pe.id_proposta', mainParams)` and pushing the returned clause into `mainConditions`
3. This ensures ALL queries (total, byStatus, byExecRange, table) are scoped to Projetus proposals only
4. Note: Some projetos_execucao rows may have NULL id_proposta — those will be excluded by the IN clause, which is correct behavior (if it's not in the whitelist, it should not appear)

Do NOT change any other logic (tipo filter, ano filter, pagination, etc.) — just add the whitelist as an additional mandatory WHERE condition.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov && npx tsc --noEmit --project web/tsconfig.json 2>&1 | head -20</automated>
    <manual>Visit /tgov page, verify both tabs show only Projetus proposals. Check that total counts are significantly reduced compared to before (should be ~250 on aprovacao, and a subset on execucao).</manual>
  </verify>
  <done>Both TGov API routes filter by PROJETUS_PROPOSAL_IDS whitelist. Only the ~250 specified proposals appear in aprovacao tab, and only their linked convenios appear in execucao tab.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. Both API routes import and use the Projetus whitelist
3. The whitelist is defined exactly once in tgov.ts
4. No SQL injection risk — all IDs are parameterized
</verification>

<success_criteria>
- TGov aprovacao tab shows ONLY proposals whose transfer_gov_id is in the whitelist
- TGov execucao tab shows ONLY convenios whose id_proposta is in the whitelist
- The whitelist contains exactly 250 unique proposal IDs (251 entries minus 1 duplicate of 11225)
- No changes to filter logic, pagination, or other features
</success_criteria>

<output>
After completion, create `.planning/quick/260401-e9r-tgov-execucao-projetus-proposals-filter/260401-e9r-SUMMARY.md`
</output>
