---
phase: 14-data-audit-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/scripts/audit-phase14.js
  - .planning/phases/14-data-audit-foundation/14-AUDIT-RESULTS.md
autonomous: true
requirements: [DATA-07]

must_haves:
  truths:
    - "NULL proposta_id count in convenios WHERE situacao ILIKE '%execu%' is known and documented"
    - "CNPJ length distribution in proponentes is known — short_cnpj count documented"
    - "If short CNPJs exist, LPAD migration is applied and verified (all cnpj LENGTH = 14)"
    - "Gap-handling strategy for Phase 15 is explicitly documented: LEFT JOIN with join_miss_count"
  artifacts:
    - path: "web/scripts/audit-phase14.js"
      provides: "One-shot diagnostic + migration script"
      min_lines: 80
    - path: ".planning/phases/14-data-audit-foundation/14-AUDIT-RESULTS.md"
      provides: "Documented audit results and gap-handling decision"
      contains: "Gap-Handling Strategy"
  key_links:
    - from: "web/scripts/audit-phase14.js"
      to: "Supabase PostgreSQL (convenios, proponentes tables)"
      via: "pg Pool using DATABASE_URL from .env.local"
      pattern: "pool\\.query"
---

<objective>
Run one-time data integrity diagnostics on the existing `convenios` and `proponentes` tables, apply CNPJ zero-padding migration if needed, and document the results with the gap-handling strategy for Phase 15.

Purpose: Eliminate the two highest-risk unknowns (NULL proposta_id scope and CNPJ padding mismatches) before any ETL or API code is written. These are one-way doors — baking the wrong join strategy into Phase 15 is harder to fix than discovering it now.

Output: A diagnostic script (`web/scripts/audit-phase14.js`) and a results document (`14-AUDIT-RESULTS.md`) that Phase 15 planning will reference.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-data-audit-foundation/14-RESEARCH.md
@web/schema.sql
@web/src/lib/db.ts
@web/scripts/diagnose-status.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run Phase 14 data audit diagnostic script</name>
  <files>web/scripts/audit-phase14.js</files>
  <read_first>
    - web/schema.sql (lines 21-47 for proponentes table, lines 130-158 for convenios table — verify column names and types)
    - web/scripts/diagnose-status.js (reference for the .env.local parsing pattern and pg Pool setup pattern used in project diagnostic scripts)
    - web/src/lib/db.ts (verify DATABASE_URL env var name)
  </read_first>
  <action>
Create `web/scripts/audit-phase14.js` using the same pattern as existing diagnostic scripts (CommonJS, .env.local manual parsing, pg Pool with ssl rejectUnauthorized false).

The script must run 5 sequential steps and print results to stdout:

**STEP 1 — NULL proposta_id diagnostic:**
```sql
SELECT
  COUNT(*) AS total_em_execucao,
  SUM(CASE WHEN (proposta_id IS NULL OR proposta_id = '') THEN 1 ELSE 0 END) AS null_proposta_id,
  SUM(CASE WHEN (proposta_id IS NOT NULL AND proposta_id != '') THEN 1 ELSE 0 END) AS has_proposta_id
FROM convenios
WHERE situacao ILIKE '%execu%';
```
Print: total_em_execucao, null_proposta_id count, has_proposta_id count.

**STEP 2 — CNPJ length distribution diagnostic:**
```sql
SELECT
  COUNT(*) AS total_proponentes,
  SUM(CASE WHEN LENGTH(cnpj) < 14 THEN 1 ELSE 0 END) AS short_cnpj,
  SUM(CASE WHEN LENGTH(cnpj) = 14 THEN 1 ELSE 0 END) AS correct_cnpj,
  SUM(CASE WHEN LENGTH(cnpj) > 14 THEN 1 ELSE 0 END) AS long_cnpj,
  MIN(LENGTH(cnpj)) AS min_len,
  MAX(LENGTH(cnpj)) AS max_len
FROM proponentes;
```
Print: total_proponentes, short_cnpj count, correct_cnpj count, long_cnpj count, min_len, max_len.

**STEP 3 — CNPJ collision pre-flight check (only if short_cnpj > 0):**
```sql
SELECT LPAD(cnpj, 14, '0') AS normalized_cnpj, COUNT(*) AS count
FROM proponentes
WHERE LENGTH(cnpj) < 14
GROUP BY normalized_cnpj
HAVING COUNT(*) > 1;
```
Print: collision count. If collisions exist, print a WARNING and DO NOT proceed with LPAD migration. If no collisions, proceed to STEP 4.

**STEP 4 — CNPJ LPAD migration (only if short_cnpj > 0 AND no collisions):**
```sql
UPDATE proponentes
SET cnpj = LPAD(cnpj, 14, '0')
WHERE LENGTH(cnpj) < 14;
```
Print: rows affected.

**STEP 5 — Post-migration verification (only if migration ran):**
```sql
SELECT COUNT(*) AS remaining_short FROM proponentes WHERE LENGTH(cnpj) < 14;
```
Print: remaining_short (must be 0).

If short_cnpj was 0 in STEP 2, skip STEPS 3-5 and print "All CNPJs already 14 characters — no migration needed."

The script must call `pool.end()` in a finally block and exit with code 0 on success, 1 on error.

Print a final summary block at the end with all key numbers formatted clearly for documentation.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && node scripts/audit-phase14.js 2>&1 | tail -20</automated>
    <manual>Script runs without errors, prints diagnostic results for both queries</manual>
  </verify>
  <acceptance_criteria>
    - web/scripts/audit-phase14.js exists and is at least 80 lines
    - File contains `const { Pool } = require('pg')` (CommonJS pattern matching existing scripts)
    - File contains `situacao ILIKE '%execu%'` (NULL proposta_id diagnostic)
    - File contains `LENGTH(cnpj) < 14` (CNPJ length diagnostic)
    - File contains `LPAD(cnpj, 14, '0')` (CNPJ migration query)
    - File contains `pool.end()` in a finally block
    - Script exits with code 0 when run via `node scripts/audit-phase14.js`
  </acceptance_criteria>
  <done>Diagnostic script runs successfully against production DB, prints NULL proposta_id count and CNPJ distribution, applies LPAD migration if needed, verifies post-migration state</done>
</task>

<task type="auto">
  <name>Task 2: Document audit results and gap-handling strategy</name>
  <files>.planning/phases/14-data-audit-foundation/14-AUDIT-RESULTS.md</files>
  <read_first>
    - The stdout output from Task 1 (the diagnostic script just ran — use the printed numbers)
    - .planning/STATE.md (verify the locked decision: "LEFT JOIN with join_miss_count for proposta join")
    - .planning/phases/14-data-audit-foundation/14-RESEARCH.md (Pattern 1 interpretation rules and Pattern 4 UPSERT documentation requirements)
  </read_first>
  <action>
Create `.planning/phases/14-data-audit-foundation/14-AUDIT-RESULTS.md` with the following structure. Fill in the actual numbers from the diagnostic script output.

```markdown
# Phase 14 Data Audit Results

**Run date:** {today's date}
**Script:** web/scripts/audit-phase14.js

## 1. NULL proposta_id in "em execucao" Convenios

| Metric | Value |
|--------|-------|
| Total convenios em execucao | {from STEP 1} |
| With proposta_id | {from STEP 1} |
| NULL or empty proposta_id | {from STEP 1} |

## 2. CNPJ Length Distribution in Proponentes

| Metric | Value |
|--------|-------|
| Total proponentes | {from STEP 2} |
| Correct (14 chars) | {from STEP 2} |
| Short (< 14 chars) | {from STEP 2} |
| Long (> 14 chars) | {from STEP 2} |
| Min length | {from STEP 2} |
| Max length | {from STEP 2} |

### CNPJ Migration Applied

{If short_cnpj > 0: "Yes — {N} rows updated via LPAD(cnpj, 14, '0'). Post-migration verification: 0 remaining short CNPJs."}
{If short_cnpj = 0: "No — all CNPJs already 14 characters. No migration needed."}

## 3. Gap-Handling Strategy for Phase 15

**Decision:** Phase 15 ETL MUST use LEFT JOIN with join_miss_count logging regardless of the NULL proposta_id count.

**Rationale:** The diagnostic result is a point-in-time snapshot. Future Python ETL runs may insert new convenios with NULL proposta_id. The architecture decision is permanent; the diagnostic count is transient. LEFT JOIN with logged miss count is the safe default — it makes data gaps visible instead of silently dropping records (the known anti-pattern from instruments/route.ts INNER JOIN).

**Reference:** STATE.md locked decision: "LEFT JOIN with join_miss_count for proposta join — INNER JOIN silently drops projects with NULL proposta_id; LEFT JOIN + logged miss count makes data loss visible"
```

Use the actual diagnostic numbers from Task 1 output. Do not use placeholders.
  </action>
  <verify>
    <automated>grep -c "Gap-Handling Strategy" /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/.planning/phases/14-data-audit-foundation/14-AUDIT-RESULTS.md</automated>
    <manual>Document contains actual numbers from diagnostic run, not placeholders</manual>
  </verify>
  <acceptance_criteria>
    - .planning/phases/14-data-audit-foundation/14-AUDIT-RESULTS.md exists
    - File contains "## 1. NULL proposta_id" section with a table containing numeric values (not placeholders like {from STEP 1})
    - File contains "## 2. CNPJ Length Distribution" section with a table containing numeric values
    - File contains "## 3. Gap-Handling Strategy" section
    - File contains "LEFT JOIN with join_miss_count" (the locked decision)
    - File contains "Run date:" with an actual date (not a placeholder)
  </acceptance_criteria>
  <done>Audit results documented with actual production numbers. Gap-handling strategy explicitly states LEFT JOIN with join_miss_count. Phase 15 planning can reference this document for join strategy decisions.</done>
</task>

</tasks>

<verification>
1. `web/scripts/audit-phase14.js` exists, runs without errors, and exits 0
2. `14-AUDIT-RESULTS.md` contains actual diagnostic numbers (not placeholders)
3. If any short CNPJs existed, `SELECT COUNT(*) FROM proponentes WHERE LENGTH(cnpj) < 14` now returns 0
4. The gap-handling strategy document explicitly says "LEFT JOIN with join_miss_count"
</verification>

<success_criteria>
- NULL proposta_id count is known and documented with real numbers
- CNPJ length distribution is known; if short CNPJs existed, they are now LPAD-migrated to 14 chars
- Gap-handling strategy for Phase 15 is committed to git as a documented decision
- No production data was corrupted (diagnostic queries are read-only; LPAD migration is idempotent)
</success_criteria>

<output>
After completion, create `.planning/phases/14-data-audit-foundation/14-01-SUMMARY.md`
</output>
