---
phase: 09-polish-production-readiness
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/dashboard/pages/home.py
  - src/dashboard/pages/qualificacao.py
  - src/dashboard/pages/lead_profile.py
  - src/dashboard/pages/propostas.py
  - src/dashboard/pages/programas.py
  - src/dashboard/pages/apoiadores.py
  - src/dashboard/pages/emendas.py
  - src/dashboard/components/badges.py
autonomous: false

must_haves:
  truths:
    - "All 7 pages use consistent premium styling — no page looks like default Streamlit"
    - "Proposta situacao values display as colored pill badges instead of raw text"
    - "Value tier indicators use pill badges (Alto Valor=green, Medio=blue, Baixo=amber, Muito Baixo=gray)"
    - "Data freshness shows as a pill badge (Atualizado=green, Desatualizado=amber, Critico=red)"
    - "Mobile layout works correctly with cards stacking and tables scrolling"
  artifacts:
    - path: "src/dashboard/components/badges.py"
      provides: "Updated badge functions using pill_badge styling"
      contains: "pill_badge"
    - path: "src/dashboard/pages/propostas.py"
      provides: "Situacao column rendered with pill badges"
      contains: "pill_badge"
  key_links:
    - from: "all page files"
      to: "src/dashboard/components/badges.py"
      via: "pill_badge import for status rendering"
      pattern: "from.*badges.*import.*pill_badge"
---

<objective>
Apply final consistency pass across all pages — integrate pill badges for status/tier/freshness, audit styling uniformity, and verify mobile responsiveness.

Purpose: Ensures every page in the dashboard meets premium quality standards with no default-Streamlit pages remaining. Pill badges replace raw text for all categorical status values.
Output: All 7 pages with consistent premium styling, pill badges for status indicators, human verification of mobile layout.
</objective>

<execution_context>
@/Users/pauloloureiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauloloureiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-production-readiness/09-RESEARCH.md
@.planning/phases/09-polish-production-readiness/09-01-SUMMARY.md
@.planning/phases/09-polish-production-readiness/09-02-SUMMARY.md
@src/dashboard/components/badges.py
@src/dashboard/components/_styles.py
@src/dashboard/pages/home.py
@src/dashboard/pages/propostas.py
@src/dashboard/pages/qualificacao.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate pill badges for status indicators and audit styling consistency</name>
  <files>
    src/dashboard/components/badges.py
    src/dashboard/pages/home.py
    src/dashboard/pages/qualificacao.py
    src/dashboard/pages/lead_profile.py
    src/dashboard/pages/propostas.py
    src/dashboard/pages/programas.py
    src/dashboard/pages/apoiadores.py
    src/dashboard/pages/emendas.py
  </files>
  <action>
1. **Update `badges.py`** to add pill badge integration for status values:
   - Add `situacao_pill(situacao_text)` that maps proposta situacao values to pill badge variants:
     - "Aprovada"/"Concluída" → green
     - "Em Análise"/"Em Execução" → blue
     - "Rejeitada"/"Cancelada" → red
     - Other/unknown → gray
   - Add `tier_pill(tier_text)` that maps value tiers:
     - "Alto Valor"/"Muito Alto" → green
     - "Medio Valor" → blue
     - "Baixo Valor" → amber
     - "Muito Baixo" → gray
   - Add `freshness_pill(freshness_text)` that maps data freshness:
     - "Atualizado" (< 24h) → green
     - "Desatualizado" (24h-72h) → amber
     - "Critico" (> 72h) → red

2. **Home page:** Replace any remaining raw freshness text with `freshness_pill()`. Ensure all KPIs use `kpi_row()` (should be done from Phase 7, verify).

3. **Qualificacao page:** Replace value tier text in ranking cards or table with `tier_pill()`. Verify ranking cards use premium styling.

4. **Lead Profile page:** Use `tier_pill()` for the tier badge display. Use `situacao_pill()` in propostas tab if situacao column is shown.

5. **Propostas page:** If situacao column is displayed in the dataframe, add a column with pill badge HTML or use `st.column_config` to style it. If raw dataframe, add a note about column formatting.

6. **Entity pages audit (programas, apoiadores, emendas):** Verify each page has:
   - Icon-enhanced section header (### icon Title pattern from Phase 7)
   - Premium KPI cards via `kpi_row()` (not raw st.metric())
   - Dark theme compatible colors (no light-mode hardcoded values)
   - Consistent spacing (st.markdown('') between sections)
   If any page is missing these, add them.

7. **Final visual consistency check:**
   - No raw `st.metric()` calls remaining (should all be `kpi_row()`)
   - No light-mode colors (white backgrounds, black text)
   - All headers use markdown with icon enhancement where appropriate
   - Sidebar search and navigation consistent across pages
  </action>
  <verify>
Grep all page files for `st.metric(` — should return zero results (all replaced with kpi_row). Grep badges.py for `situacao_pill`, `tier_pill`, `freshness_pill` to confirm all 3 exist. Run `python -c "from src.dashboard.components.badges import situacao_pill, tier_pill, freshness_pill; print('OK')"` to verify imports.
  </verify>
  <done>All pages use pill badges for status indicators. No st.metric() calls remain. All pages have icon headers, premium KPIs, dark-compatible colors, and consistent spacing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete polish layer</name>
  <files>src/dashboard/pages/home.py</files>
  <action>
Human verification of the complete Phase 9 polish layer across all 7 dashboard pages.

What was built:
- Mobile responsive layouts (CSS breakpoints at 600px/768px)
- Loading spinners on all data queries
- Empty state messages with actionable Portuguese guidance
- Card hover effects and page fade-in animations
- Pill badges for proposta situacao, value tier, and data freshness
- Consistent premium styling on every page

How to verify:
1. Start the dashboard: `cd /Users/pauloloureiro/Desktop/Work/Sigma/Projects/Projetus && streamlit run src/dashboard/streamlit_app.py`
2. Desktop verification: Navigate all 7 pages — no default Streamlit. Hover cards for lift/glow. Check pill badges. Verify spinners.
3. Mobile verification (Chrome DevTools F12 → Ctrl+Shift+M → iPhone 14 Pro 390px): KPI cards stack. Tables scroll horizontally. No overflow.
4. Empty state verification: Qualificacao extreme filters → Portuguese message. Lead Profile invalid CNPJ → warning.
5. Accessibility: Chrome DevTools prefers-reduced-motion → animations disabled.
  </action>
  <verify>Human visually confirms all 7 Phase 9 success criteria from ROADMAP.md are satisfied.</verify>
  <done>All pages verified as premium with responsive layout, loading states, empty states, animations, pill badges, and consistent styling.</done>
</task>

</tasks>

<verification>
1. Zero `st.metric()` calls across all pages (replaced by `kpi_row()`)
2. Pill badges render for proposta situacao, value tier, and data freshness
3. All 7 pages pass visual consistency (dark theme, icon headers, premium KPIs)
4. Mobile layout stacks cards and scrolls tables at 600px/768px breakpoints
5. Loading spinners visible during data fetch
6. Empty states show Portuguese messages with actionable guidance
7. Hover animations smooth on desktop, disabled on touch devices
8. Reduced motion preference respected
</verification>

<success_criteria>
- Human verifies all 7 pages look premium (no default Streamlit)
- Mobile responsive layout confirmed in DevTools
- Pill badges visible for status/tier/freshness indicators
- Loading and empty states confirmed working
- Animation accessibility verified
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-production-readiness/09-03-SUMMARY.md`
</output>
