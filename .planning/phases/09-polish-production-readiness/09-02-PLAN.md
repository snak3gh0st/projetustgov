---
phase: 09-polish-production-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/pages/home.py
  - src/dashboard/pages/qualificacao.py
  - src/dashboard/pages/lead_profile.py
  - src/dashboard/pages/propostas.py
  - src/dashboard/pages/programas.py
  - src/dashboard/pages/apoiadores.py
  - src/dashboard/pages/emendas.py
autonomous: true

must_haves:
  truths:
    - "No page shows a blank screen while data loads — all queries wrapped in st.spinner()"
    - "Every filtered data table shows a friendly Portuguese message when no results match"
    - "Empty state messages include actionable suggestions (adjust filters, clear selection)"
    - "Loading spinner text is in Portuguese (Carregando...)"
  artifacts:
    - path: "src/dashboard/pages/home.py"
      provides: "Loading states for entity counts and charts"
      contains: "st.spinner"
    - path: "src/dashboard/pages/qualificacao.py"
      provides: "Empty state for filtered proponent table"
      contains: "empty"
    - path: "src/dashboard/pages/lead_profile.py"
      provides: "Loading states for profile data, empty states for tabs"
      contains: "st.spinner"
  key_links:
    - from: "all page files"
      to: "database queries"
      via: "st.spinner() wrapping query calls"
      pattern: "st\\.spinner.*Carregando"
---

<objective>
Add loading states (st.spinner) and empty state messages across all 7 dashboard pages.

Purpose: Eliminates blank screens during data loading and provides helpful guidance when no data matches filters. Users always see either a loading indicator or actionable feedback.
Output: All 7 page files updated with st.spinner() wrapping queries and conditional empty state rendering.
</objective>

<execution_context>
@/Users/pauloloureiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauloloureiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-production-readiness/09-RESEARCH.md
@src/dashboard/pages/home.py
@src/dashboard/pages/qualificacao.py
@src/dashboard/pages/lead_profile.py
@src/dashboard/pages/propostas.py
@src/dashboard/pages/programas.py
@src/dashboard/pages/apoiadores.py
@src/dashboard/pages/emendas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loading states and empty states to Home, Qualificacao, and Lead Profile</name>
  <files>
    src/dashboard/pages/home.py
    src/dashboard/pages/qualificacao.py
    src/dashboard/pages/lead_profile.py
  </files>
  <action>
For each page, audit every database query call and wrap in `st.spinner()`:

**home.py:**
- Wrap entity count queries in `with st.spinner("Carregando resumo..."):`
- Wrap chart data queries (choropleth, trend) in `with st.spinner("Carregando gráficos..."):`
- Add empty state check: if entity counts return 0 total, show `st.info("Nenhum dado encontrado. Verifique se a extração foi executada.")`
- Check if sparkline data queries need wrapping

**qualificacao.py:**
- Wrap proponent query in `with st.spinner("Carregando proponentes..."):`
- After filtering, check `if filtered_df.empty:` → show `st.info("Nenhum proponente encontrado para os filtros selecionados.")` with `st.caption("Sugestões:\n- Ajuste o filtro de valor mínimo\n- Expanda o estado selecionado\n- Limpe todos os filtros")`
- Ensure the ranked table only renders when data exists

**lead_profile.py:**
- Wrap lead overview query in `with st.spinner("Carregando perfil do lead..."):`
- For each tab (emendas, propostas, ministerios, programas), wrap data query in spinner
- Add empty states per tab: e.g., `st.info("Nenhuma emenda encontrada para este proponente.")` with contextual caption
- Handle case where CNPJ is invalid or not found: `st.warning("Lead não encontrado. Verifique o CNPJ informado.")`

**Important:** Do NOT double-wrap queries that already have st.spinner(). Check first. Use Portuguese for ALL user-facing text. Keep spinner text short (under 40 chars).
  </action>
  <verify>
Grep each file for `st.spinner` to confirm coverage. Grep each file for `.empty` or `len(.*) == 0` to confirm empty state checks. Run `python -c "import src.dashboard.pages.home"` to verify no import errors.
  </verify>
  <done>Home, Qualificacao, and Lead Profile pages have st.spinner() on all queries and friendly Portuguese empty state messages with actionable suggestions.</done>
</task>

<task type="auto">
  <name>Task 2: Add loading states and empty states to entity pages (Propostas, Programas, Apoiadores, Emendas)</name>
  <files>
    src/dashboard/pages/propostas.py
    src/dashboard/pages/programas.py
    src/dashboard/pages/apoiadores.py
    src/dashboard/pages/emendas.py
  </files>
  <action>
Apply the same loading/empty state pattern to all 4 entity pages:

**For ALL entity pages (propostas, programas, apoiadores, emendas):**

1. **Loading states:** Wrap the main data query in `with st.spinner("Carregando {entity_name}..."):`
   - propostas.py: "Carregando propostas..."
   - programas.py: "Carregando programas..."
   - apoiadores.py: "Carregando apoiadores..."
   - emendas.py: "Carregando emendas..."

2. **Empty states after filtering:** After applying any filters, check `if filtered_df.empty:` and show:
   - `st.info("Nenhum(a) {entity} encontrado(a) para os filtros selecionados.")`
   - `st.caption("Tente ajustar os filtros ou limpar a seleção para ver todos os dados.")`

3. **No data at all:** If the initial query returns empty (before filtering), show:
   - `st.warning("Nenhum dado de {entity} disponível. Verifique se a extração foi executada.")`

4. **Cross-filter empty state:** If a page is filtered by `session_state.selected_proposta_id` and no results match:
   - `st.info("Nenhum(a) {entity} vinculado(a) à proposta selecionada.")`
   - `st.caption("Limpe a seleção na página de Propostas para ver todos os dados.")`

**Important:** Preserve existing CSV export logic — only show export button when data exists. Do NOT wrap st.dataframe() inside spinner (only the query). Use gender-appropriate Portuguese (nenhuma proposta, nenhum programa, nenhum apoiador, nenhuma emenda).
  </action>
  <verify>
Grep all 4 files for `st.spinner` and `.empty` to confirm coverage. Run `python -c "import src.dashboard.pages.propostas; import src.dashboard.pages.programas; import src.dashboard.pages.apoiadores; import src.dashboard.pages.emendas"` to verify no import errors.
  </verify>
  <done>All 4 entity pages have loading spinners on queries, empty state messages after filtering, no-data warnings, and cross-filter empty states — all in Portuguese with actionable guidance.</done>
</task>

</tasks>

<verification>
1. All 7 page files have `st.spinner()` wrapping database queries
2. All 7 page files have `.empty` checks with Portuguese messages
3. Empty state messages include actionable suggestions (filter adjustment, selection clearing)
4. No import errors in any page file
5. CSV export only visible when data exists
</verification>

<success_criteria>
- Zero blank screens during data loading (all queries wrapped in st.spinner)
- Every filtered table has empty state handling with Portuguese message
- Empty state messages provide context-specific actionable guidance
- Cross-filter empty states explain how to clear selection
- Loading spinner text in Portuguese, under 40 characters
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-production-readiness/09-02-SUMMARY.md`
</output>
