---
phase: 11-lead-management-contact-tracking
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/lead/[cnpj]/page.tsx
  - web/src/components/LeadSlideOver.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Vendedor can click edit icon next to phone field to enable editing"
    - "Vendedor can click edit icon next to email field to enable editing"
    - "Edited phone/email data saves via PATCH endpoint on blur or Enter key"
    - "Visualizador role sees no edit controls (read-only access maintained)"
  artifacts:
    - path: "web/src/app/lead/[cnpj]/page.tsx"
      provides: "Inline edit UI for telefone and email fields"
      min_lines: 250
    - path: "web/src/components/LeadSlideOver.tsx"
      provides: "Inline edit UI for telefone and email in slide-over"
      min_lines: 200
  key_links:
    - from: "web/src/app/lead/[cnpj]/page.tsx"
      to: "/api/leads/[cnpj]"
      via: "PATCH request on field blur/Enter"
      pattern: "fetch.*method.*PATCH"
    - from: "web/src/components/LeadSlideOver.tsx"
      to: "/api/leads/[cnpj]"
      via: "PATCH request on field blur/Enter"
      pattern: "fetch.*method.*PATCH"
---

<objective>
Add inline editing controls for phone and email contact fields on lead detail page and slide-over component.

Purpose: Enable vendedores to update outdated contact information discovered during sales calls, maintaining CRM data quality.
Output: Editable phone/email fields with save-on-blur functionality, respecting visualizador read-only permissions.
</objective>

<execution_context>
@/Users/pauloloureiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauloloureiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap closure context
@.planning/phases/11-lead-management-contact-tracking/11-VERIFICATION.md

# Files to modify
@web/src/app/lead/[cnpj]/page.tsx
@web/src/components/LeadSlideOver.tsx

# API endpoint (already exists)
@web/src/app/api/leads/[cnpj]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline edit UI for phone/email on lead detail page</name>
  <files>web/src/app/lead/[cnpj]/page.tsx</files>
  <action>
    Add inline editing capability to the contact info section (lines 142-161) on the lead detail page.

    **Implementation approach:**
    - Add state: `editingField` (null | 'telefone' | 'email') and `editValue` (string)
    - Replace static display with conditional render:
      - NOT editing: Show value + WhatsApp/mailto link + edit icon (pencil)
      - IS editing: Show input field with value, auto-focus
    - Edit icon onClick: Set `editingField` and `editValue` to current value
    - Input onBlur and onKeyDown (Enter): Call `updateContact(field, editValue)`
    - Input onKeyDown (Escape): Cancel edit without saving
    - `updateContact` function:
      ```ts
      async function updateContact(field: 'telefone' | 'email', value: string) {
        await fetch(`/api/leads/${encodeURIComponent(cnpj)}`, {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ id: first.id, [field]: value })
        })
        setProjetos(prev => prev.map(p => p.cnpj === cnpj ? {...p, [field]: value} : p))
        setEditingField(null)
      }
      ```
    - Only show edit icon when `canModify === true` (vendedor/gestor, not visualizador)
    - Keep WhatsApp/mailto links functional even in non-edit mode
    - Style input field to match dark theme: bg-sigma-navy-card, border-sigma-neon/50 on focus

    **Visual design:**
    - Edit icon: Pencil SVG (14x14), text-gray-500 hover:text-sigma-neon, cursor-pointer
    - Input field: text-sm, rounded-md, px-2 py-1, bg-sigma-navy-light border border-white/20
    - Position edit icon to the right of the field label or value
  </action>
  <verify>
    1. Run `npm run build` in web/ directory — should compile without errors
    2. Visually inspect the contact info section — edit icons appear next to phone/email when canModify=true
    3. Check that visualizador users (canModify=false) see no edit icons
  </verify>
  <done>
    - Lead detail page displays edit icons next to phone/email fields for vendedor/gestor
    - Clicking edit icon converts field to input with auto-focus
    - Blur or Enter key saves changes via PATCH endpoint
    - Escape key cancels editing
    - WhatsApp/mailto links remain functional when not editing
    - Visualizador sees no edit controls
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inline edit UI for phone/email in LeadSlideOver component</name>
  <files>web/src/components/LeadSlideOver.tsx</files>
  <action>
    Add inline editing capability to the contact section (lines 98-120) in the LeadSlideOver component.

    **Implementation approach:**
    - Add state: `editingField` (null | 'telefone' | 'email'), `editValue` (string)
    - Add prop: `canModify: boolean` (passed from parent, determines if edit icons show)
    - Replace static phone/email display with conditional render:
      - NOT editing: Show icon + value + edit icon (pencil)
      - IS editing: Show icon + input field
    - Edit icon onClick: Set `editingField` and `editValue`
    - Input onBlur and onKeyDown (Enter): Call `updateContact(field, editValue)`
    - Input onKeyDown (Escape): Cancel edit
    - `updateContact` function:
      ```ts
      async function updateContact(field: 'telefone' | 'email', value: string) {
        await fetch(`/api/leads/${encodeURIComponent(lead.cnpj)}`, {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ id: lead.id, [field]: value })
        })
        // Update local lead object via callback prop or parent refresh
        setEditingField(null)
      }
      ```
    - Only show edit icon when `canModify === true`
    - Update parent component (leads/page.tsx) to pass `canModify` prop to LeadSlideOver

    **Visual design:**
    - Match existing slide-over styling: text-sm text-gray-300
    - Input field: bg-sigma-navy-light, border border-white/20, rounded-md, px-2 py-1
    - Edit icon: Same as Task 1 (pencil, 14x14, gray-500 hover:sigma-neon)

    **Note:** Since LeadSlideOver receives a `lead` prop (not state), after successful update you may need to trigger parent refresh or accept an `onUpdate` callback. For MVP, optimistic update on client side is acceptable.
  </action>
  <verify>
    1. Run `npm run build` in web/ directory — should compile without errors
    2. Visually inspect LeadSlideOver contact section — edit icons appear when canModify=true
    3. Verify clicking edit icon converts to input field
  </verify>
  <done>
    - LeadSlideOver displays edit icons next to phone/email for vendedor/gestor
    - Clicking edit icon converts field to input with auto-focus
    - Blur or Enter saves via PATCH endpoint
    - Escape cancels editing
    - Visualizador sees no edit controls in slide-over
    - Quick action buttons (WhatsApp, Email) remain functional
  </done>
</task>

</tasks>

<verification>
## Code Verification

1. **TypeScript compilation:** `npm run build` passes without errors
2. **Grep for edit pattern:** Find PATCH calls in both files
   ```bash
   grep -n "method.*PATCH" web/src/app/lead/[cnpj]/page.tsx web/src/components/LeadSlideOver.tsx
   ```
3. **Permission check:** Verify edit icons only render when `canModify === true`

## Functional Verification (Human Required)

1. **Login as vendedor** (Elison, Wellington, or Gabriel)
   - Visit lead detail page (`/lead/[cnpj]`)
   - Verify edit icons appear next to phone and email fields
   - Click edit icon → field becomes input → edit value → press Enter → value saves
   - Verify WhatsApp link still works with updated phone number

2. **Login as visualizador** (Paulo)
   - Visit same lead detail page
   - Verify NO edit icons appear
   - Verify phone/email are still visible but read-only

3. **Test slide-over edit**
   - Click lead from leads list to open slide-over
   - Verify edit icons appear for vendedor/gestor
   - Test inline edit flow (click icon, edit, save)

4. **Test error handling**
   - Edit phone/email with invalid value (e.g., empty string)
   - Verify API handles gracefully (400 or validation error)
</verification>

<success_criteria>
- Phone and email fields on lead detail page have edit icons next to them (when canModify=true)
- Clicking edit icon converts field to inline input with current value pre-filled and auto-focused
- Pressing Enter or blur event triggers PATCH request to `/api/leads/[cnpj]` with updated field
- Pressing Escape cancels editing without saving
- WhatsApp and mailto links remain functional after editing
- LeadSlideOver component has same inline edit functionality
- Visualizador role sees no edit icons (read-only access maintained)
- All TypeScript types compile without errors
- Gap in VERIFICATION.md (Truth #8) is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/11-lead-management-contact-tracking/11-05-SUMMARY.md`
</output>
