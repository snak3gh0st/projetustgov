---
phase: 08-lead-profile-enhanced-navigation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/dashboard/pages/lead_profile.py
  - src/dashboard/streamlit_app.py
autonomous: true

must_haves:
  truths:
    - "Lead profile page displays proponente name, contact info (email, telefone, endereco), and value tier badge"
    - "Lead profile shows KPI summary cards (tier, total emendas value, propostas count, convenios)"
    - "Lead profile organizes related data in tabs (Emendas, Propostas, Ministerios, Programas)"
    - "Quick actions available: CSV export of lead data and copy CNPJ display"
    - "Lead profile page accessible via st.navigation and session_state routing"
  artifacts:
    - path: "src/dashboard/pages/lead_profile.py"
      provides: "Dedicated lead profile page with tabs and KPIs"
      contains: "render_lead_profile"
    - path: "src/dashboard/streamlit_app.py"
      provides: "Navigation entry for lead profile page"
      contains: "lead_profile"
  key_links:
    - from: "src/dashboard/pages/lead_profile.py"
      to: "src/dashboard/queries/lead_profile.py"
      via: "import query functions"
      pattern: "from src.dashboard.queries.lead_profile import"
    - from: "src/dashboard/pages/lead_profile.py"
      to: "src/dashboard/components/kpi.py"
      via: "kpi_row for value summary"
      pattern: "from src.dashboard.components.kpi import kpi_row"
    - from: "src/dashboard/streamlit_app.py"
      to: "src/dashboard/pages/lead_profile.py"
      via: "st.Page registration"
      pattern: "st.Page.*lead_profile"
---

<objective>
Build the dedicated lead profile page with tabbed data sections, KPI value summary, contact info, and quick actions. Register it in the app navigation.

Purpose: Delivers LEAD-01 (dedicated profile page), LEAD-03 (contact info), LEAD-04 (value assessment), LEAD-05 (ministerios/programas associations), LEAD-06 (quick actions). This is the core sales workflow page for deep-dive proponent research.
Output: New lead_profile.py page fully wired into the Streamlit app navigation.
</objective>

<execution_context>
@/Users/pauloloureiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauloloureiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-lead-profile-enhanced-navigation/08-RESEARCH.md
@.planning/phases/08-lead-profile-enhanced-navigation/08-01-SUMMARY.md
@src/dashboard/streamlit_app.py
@src/dashboard/pages/qualificacao_new.py
@src/dashboard/components/kpi.py
@src/dashboard/components/_styles.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lead profile page with tabs, KPIs, and quick actions</name>
  <files>src/dashboard/pages/lead_profile.py</files>
  <action>
Create `src/dashboard/pages/lead_profile.py` with a `render_lead_profile()` function:

**Guard clause:** Check `st.session_state.get("selected_lead_cnpj")`. If None, show `st.warning("Nenhum lead selecionado. Use a busca global ou a pagina de Qualificacao para selecionar um proponente.")` and return early.

**Header section:**
- Fetch lead data: `get_lead_overview(cnpj)` from lead_profile queries.
- If empty DataFrame, show error and return.
- Display `st.title(lead_data["nome"])` with tier badge.
- Use `kpi_row()` (from components.kpi) for value assessment summary: 4 cards showing Tier (with color from TIER_COLORS), Total Emendas (formatted as R$ XM), Total Propostas (count), Convenios (count).

**Contact info section:**
- Render email, telefone, endereco/municipio/estado in a styled section using `st.columns(3)`.
- Use `st.markdown()` with bold labels. Show "Nao disponivel" for missing fields.

**Quick actions row:**
- `st.columns(3)` with:
  1. `st.download_button()` to export lead overview as CSV (single-row DataFrame with all proponente fields). Label: "Exportar Lead (CSV)".
  2. Display CNPJ in a `st.code()` block (which has built-in copy button). Format as XX.XXX.XXX/XXXX-XX.
  3. Button to navigate back to Qualificacao: `st.button("Voltar para Qualificacao")` that calls `st.switch_page()`.

**Tabbed content (st.tabs):**
- 4 tabs: ["Emendas", "Propostas", "Ministerios/Orgaos", "Programas"]
- **Emendas tab:** Call `get_lead_emendas(cnpj, limit=100)`. Format valor_emenda as R$ currency. Show count caption ("Mostrando X emendas"). Display with `st.dataframe()`.
- **Propostas tab:** Call `get_lead_propostas(cnpj, limit=100)`. Format valor_global as R$ currency. Show count caption. Display with `st.dataframe()`.
- **Ministerios tab:** Call `get_lead_ministerios(cnpj)`. Display as simple table showing orgao name and count of associated propostas.
- **Programas tab:** Call `get_lead_programas(cnpj)`. Display as simple table showing programa name and count.

**Important patterns:**
- Import `kpi_row` from `src.dashboard.components.kpi` (Phase 6 pattern).
- Import `calculate_value_tier, TIER_COLORS` from `src.dashboard.utils.tiers`.
- Import all query functions from `src.dashboard.queries.lead_profile`.
- Use the same `format_cnpj()` function pattern from qualificacao_new.py (copy it or import).
- Add `st.markdown("")` between sections for spacing (Phase 7 pattern).
- Use icon-enhanced section headers: `st.markdown("### Section Title")` (Phase 7 pattern).
- All st.dataframe() calls use `use_container_width=True, hide_index=True`.
  </action>
  <verify>Run `python -c "from src.dashboard.pages.lead_profile import render_lead_profile; print('OK')"` — should import without error.</verify>
  <done>Lead profile page renders proponente header with tier badge, contact info, KPI value summary, quick actions (CSV export, CNPJ copy, back navigation), and 4 tabbed data sections (emendas, propostas, ministerios, programas).</done>
</task>

<task type="auto">
  <name>Task 2: Register lead profile page in app navigation</name>
  <files>src/dashboard/streamlit_app.py</files>
  <action>
Update `src/dashboard/streamlit_app.py`:

1. Add session state initialization for lead profile context (after existing session state block):
```python
if "selected_lead_cnpj" not in st.session_state:
    st.session_state.selected_lead_cnpj = None
if "selected_lead_name" not in st.session_state:
    st.session_state.selected_lead_name = None
```

2. Add lead_profile_page function (after qualificacao_page):
```python
def lead_profile_page():
    """Dedicated lead profile page for deep-dive proponent research."""
    from src.dashboard.pages.lead_profile import render_lead_profile
    render_lead_profile()
```

3. Add to pages list (after Qualificacao entry):
```python
st.Page(lead_profile_page, title="Lead Profile", icon="👤"),
```

Do NOT modify any existing page functions or navigation entries. Only ADD the new page and session state.
  </action>
  <verify>Run `python -c "import src.dashboard.streamlit_app"` to verify no import errors. Check that the pages list now includes 7 entries (Home, Propostas, Programas, Apoiadores, Emendas, Qualificacao, Lead Profile).</verify>
  <done>Lead Profile page registered in st.navigation with session state keys for selected_lead_cnpj and selected_lead_name initialized at app startup.</done>
</task>

</tasks>

<verification>
1. Lead profile page imports and renders without errors
2. Session state keys initialized for lead context tracking
3. Navigation includes Lead Profile page entry
4. KPI cards display tier, emendas value, propostas count, convenios
5. Contact info section shows email, telefone, address
6. Quick actions include CSV export and CNPJ display
7. Four tabs render data tables with proper formatting
</verification>

<success_criteria>
- Navigating to Lead Profile with a selected_lead_cnpj in session_state shows complete proponente data
- Without selected_lead_cnpj, page shows helpful warning message
- All data sections populated from lead_profile queries
- CSV export downloads single-lead overview data
</success_criteria>

<output>
After completion, create `.planning/phases/08-lead-profile-enhanced-navigation/08-02-SUMMARY.md`
</output>
