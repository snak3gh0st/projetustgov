---
phase: 08-lead-profile-enhanced-navigation
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/dashboard/components/ranking_cards.py
  - src/dashboard/pages/qualificacao_new.py
  - src/dashboard/pages/propostas.py
  - src/dashboard/pages/programas.py
  - src/dashboard/pages/apoiadores.py
  - src/dashboard/pages/emendas.py
autonomous: true

must_haves:
  truths:
    - "Qualificacao page shows visual ranking cards with tier badges instead of only a raw table"
    - "All 4 entity pages (Propostas, Programas, Apoiadores, Emendas) use premium styling with icon headers and KPI cards"
    - "Clicking a lead in Qualificacao navigates to lead profile page"
  artifacts:
    - path: "src/dashboard/components/ranking_cards.py"
      provides: "Visual ranking card component for lead display"
      contains: "render_ranking_card"
    - path: "src/dashboard/pages/qualificacao_new.py"
      provides: "Enhanced qualificacao with ranking cards and lead profile navigation"
      contains: "render_ranking_card"
  key_links:
    - from: "src/dashboard/components/ranking_cards.py"
      to: "src/dashboard/components/_styles.py"
      via: "get_iframe_styles for dark theme"
      pattern: "from src.dashboard.components._styles import get_iframe_styles"
    - from: "src/dashboard/pages/qualificacao_new.py"
      to: "src/dashboard/components/ranking_cards.py"
      via: "ranking card rendering"
      pattern: "from src.dashboard.components.ranking_cards import"
    - from: "src/dashboard/pages/qualificacao_new.py"
      to: "st.switch_page"
      via: "lead profile navigation"
      pattern: "st.switch_page"
---

<objective>
Enhance the Qualificacao page with visual ranking cards and lead profile navigation. Apply consistent premium styling to all 4 entity pages (Propostas, Programas, Apoiadores, Emendas).

Purpose: Delivers NAV-01 (visual ranking cards in Qualificacao), NAV-04 (consistent premium entity page styling). Also wires Qualificacao to lead profile navigation for seamless workflow.
Output: New ranking card component, enhanced Qualificacao page, and 4 updated entity pages.
</objective>

<execution_context>
@/Users/pauloloureiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauloloureiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-lead-profile-enhanced-navigation/08-RESEARCH.md
@.planning/phases/08-lead-profile-enhanced-navigation/08-01-SUMMARY.md
@src/dashboard/pages/qualificacao_new.py
@src/dashboard/components/kpi.py
@src/dashboard/components/_styles.py
@src/dashboard/pages/propostas.py
@src/dashboard/pages/programas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ranking card component and enhance Qualificacao page</name>
  <files>src/dashboard/components/ranking_cards.py, src/dashboard/pages/qualificacao_new.py</files>
  <action>
**Create `src/dashboard/components/ranking_cards.py`:**

`render_ranking_cards(leads_df: pd.DataFrame, top_n: int = 10) -> None`:
- Renders the top N leads as visual cards using `st.html()` with embedded styles.
- Each card shows: rank number (large), lead name, CNPJ (formatted), tier badge (colored), total propostas, valor emendas (formatted as R$ XK/XM).
- Use `get_iframe_styles()` from `_styles.py` for dark theme consistency.
- Tier badge colors from `TIER_COLORS` dict in `src.dashboard.utils.tiers`.
- Cards use a CSS grid layout (similar to `kpi_row` pattern) — render ALL cards in a SINGLE `st.html()` call to avoid iframe proliferation.
- Card style: dark background with colored left border (tier color), no backdrop-filter (preserve budget). Use `background: rgba(255, 255, 255, 0.03); border-left: 4px solid {tier_color}; border-radius: 8px; padding: 1rem; margin-bottom: 0.5rem;`.
- Each card includes a "Ver Perfil" text link (styled as neon accent color).

`render_ranking_card_with_action(rank, proponente_dict) -> bool`:
- Renders a single ranking card as an interactive element.
- Returns True if "Ver Perfil" button was clicked for this card.
- Uses `st.button()` next to the card HTML for navigation trigger.

**Update `src/dashboard/pages/qualificacao_new.py`:**

Add a "Top Leads" section between the value distribution chart and the filters sidebar:

1. After the distribution chart section and before the filters sidebar section, add:
   ```python
   st.markdown("### Top Leads")
   st.caption("Clique em um lead para ver o perfil completo")
   ```

2. Import and use `calculate_value_tier` and `TIER_COLORS` from utils.tiers.

3. Show top 5 leads as ranking cards. For each card, add a `st.button(f"Ver Perfil: {nome}", key=f"lead_profile_{idx}")` button. On click:
   - Set `st.session_state.selected_lead_cnpj = cnpj`
   - Set `st.session_state.selected_lead_name = nome`
   - Call `st.switch_page("pages/lead_profile.py")`

4. Also add a "Ver Perfil" button to the existing lead detail section (where the selectbox already allows selecting a lead). When a lead is selected in the detail section, add a button below the KPI row that navigates to the full lead profile page.

5. Add tier badge to each row in the main data table. Add a `tier` column computed via `calculate_value_tier()` applied to each row: `df["tier"] = df.apply(lambda row: calculate_value_tier(row["total_propostas"], row["valor_total_emendas"] or 0, row.get("total_convenios", 0)), axis=1)`. Display it in the table.

Do NOT restructure the existing page layout. Only ADD the top leads section and the navigation buttons. Keep all existing functionality intact.
  </action>
  <verify>Run `python -c "from src.dashboard.components.ranking_cards import render_ranking_cards; print('OK')"` and `python -c "from src.dashboard.pages.qualificacao_new import render_qualificacao_nova; print('OK')"` — both import without errors.</verify>
  <done>Qualificacao page shows top 5 leads as visual ranking cards with tier badges and "Ver Perfil" navigation. Data table includes tier column. Ranking card component available for reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Apply premium styling to 4 entity pages</name>
  <files>src/dashboard/pages/propostas.py, src/dashboard/pages/programas.py, src/dashboard/pages/apoiadores.py, src/dashboard/pages/emendas.py</files>
  <action>
Apply consistent premium styling updates to all 4 entity pages. For EACH page (propostas.py, programas.py, apoiadores.py, emendas.py):

1. **Replace st.metric() with kpi_row():** Find any `st.metric()` calls and replace with `kpi_row()` from `src.dashboard.components.kpi`. Follow the Phase 7 pattern: `kpi_row([{"label": "Total", "value": count}, ...])`.

2. **Add icon-enhanced headers:** Replace plain `st.subheader()` or `st.header()` with markdown format: `st.markdown("### Icon Title")` where Icon is a relevant emoji (Phase 7 pattern). Examples:
   - Propostas: "### Propostas"
   - Programas: "### Programas"
   - Apoiadores: "### Apoiadores"
   - Emendas: "### Emendas"

3. **Add section spacing:** Add `st.markdown("")` between major sections for breathing room (Phase 7 pattern).

4. **Ensure dark-theme-compatible colors:** Check for any hardcoded light-mode colors (like `#d4edda`, `white`, `#fff`). Replace with dark-compatible alternatives (use `rgba(16, 185, 129, 0.15)` for success highlights, per Phase 6 decision).

5. **Consistent dataframe styling:** Ensure all `st.dataframe()` calls use `use_container_width=True, hide_index=True`.

IMPORTANT: Do NOT change page functionality, query logic, filter behavior, or CSV export. Only visual styling changes. Read each file fully before modifying to understand existing structure. Preserve all existing imports and function signatures.
  </action>
  <verify>Run `python -c "from src.dashboard.pages.propostas import render_propostas; from src.dashboard.pages.programas import render_programas; from src.dashboard.pages.apoiadores import render_apoiadores; from src.dashboard.pages.emendas import render_emendas; print('OK')"` — all 4 pages import without errors. Grep for "st.metric" in all 4 files — should find zero remaining instances (all replaced with kpi_row).</verify>
  <done>All 4 entity pages use kpi_row() instead of st.metric(), have icon-enhanced headers, section spacing, dark-theme-compatible colors, and consistent dataframe styling.</done>
</task>

</tasks>

<verification>
1. Qualificacao page shows top 5 ranking cards with tier badges
2. Clicking ranking card or "Ver Perfil" button navigates to lead profile
3. Data table includes tier classification column
4. All 4 entity pages use kpi_row() instead of st.metric()
5. Icon-enhanced headers and section spacing on all entity pages
6. No hardcoded light-mode colors remain
7. All pages import and render without errors
</verification>

<success_criteria>
- Sales rep sees top leads immediately on Qualificacao page with visual tier indicators
- One-click navigation from any lead to dedicated profile page
- All entity pages have consistent Sigma-branded appearance
- No regression in existing page functionality
</success_criteria>

<output>
After completion, create `.planning/phases/08-lead-profile-enhanced-navigation/08-04-SUMMARY.md`
</output>
