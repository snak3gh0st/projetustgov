---
phase: 08-lead-profile-enhanced-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/queries/search.py
  - src/dashboard/queries/lead_profile.py
  - src/dashboard/utils/__init__.py
  - src/dashboard/utils/tiers.py
autonomous: true

must_haves:
  truths:
    - "Cross-entity search query returns proponentes, propostas, and programas results ranked by relevance"
    - "Lead profile queries return complete proponente data with emendas, propostas, and ministerio associations"
    - "Tier classification assigns HIGH/MEDIUM/LOW based on propostas count and emendas value"
  artifacts:
    - path: "src/dashboard/queries/search.py"
      provides: "Cross-entity UNION ALL search function"
      contains: "search_entities"
    - path: "src/dashboard/queries/lead_profile.py"
      provides: "Lead overview, emendas, propostas, ministerios queries"
      contains: "get_lead_overview"
    - path: "src/dashboard/utils/tiers.py"
      provides: "Value tier classification logic"
      contains: "calculate_value_tier"
  key_links:
    - from: "src/dashboard/queries/search.py"
      to: "src/dashboard/config.py"
      via: "run_query import"
      pattern: "from src.dashboard.config import run_query"
    - from: "src/dashboard/queries/lead_profile.py"
      to: "src/dashboard/config.py"
      via: "run_query import"
      pattern: "from src.dashboard.config import run_query"
---

<objective>
Create the data layer for lead profile and global search: cross-entity search queries, lead-specific aggregation queries, and centralized tier classification utility.

Purpose: Provides the query foundation that the lead profile page (Plan 02) and global search component (Plan 03) depend on. Centralizes tier logic to avoid duplication across UI components.
Output: Three new modules — search queries, lead profile queries, tier utility.
</objective>

<execution_context>
@/Users/pauloloureiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauloloureiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lead-profile-enhanced-navigation/08-RESEARCH.md
@src/dashboard/config.py
@src/dashboard/queries/qualificacao.py
@src/loader/db_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-entity search queries and tier utility</name>
  <files>src/dashboard/queries/search.py, src/dashboard/utils/__init__.py, src/dashboard/utils/tiers.py</files>
  <action>
Create `src/dashboard/utils/__init__.py` (empty file) and `src/dashboard/utils/tiers.py` with a `calculate_value_tier(total_propostas: int, valor_emendas: float, total_convenios: int) -> str` function that returns "HIGH", "MEDIUM", or "LOW":
- HIGH: total_propostas == 0 (virgin proponents) OR (valor_emendas > 1_000_000 AND total_convenios > 0)
- MEDIUM: total_propostas <= 3 AND 100_000 <= valor_emendas <= 1_000_000
- LOW: everything else

Also add a `TIER_COLORS` dict mapping tier to hex colors: HIGH="#10B981", MEDIUM="#00D4FF", LOW="#6B7280" (matches Sigma semantic colors).

Create `src/dashboard/queries/search.py` with:

`search_entities(search_term: str, limit: int = 20) -> pd.DataFrame`:
- Use `run_query()` from `src.dashboard.config` (NOT raw engine — follows existing pattern).
- UNION ALL query across proponentes (relevance_score=3, filter is_osc=TRUE), propostas (relevance_score=2, match on titulo), programas (relevance_score=1, match on nome).
- Use ILIKE with `%search_term%` pattern. Use parameterized `:search_pattern` (NOT string interpolation — SQL injection risk).
- Return columns: entity_type, id, nome, cnpj, relevance_score.
- ORDER BY relevance_score DESC, nome ASC, LIMIT :limit.
- Decorate with `@st.cache_data(ttl="5m")`.

IMPORTANT: Use `run_query(query_str, params)` which accepts `text()` SQL strings and param dicts — this is the established pattern in config.py. Do NOT use pd.read_sql_query directly.
  </action>
  <verify>
Run `python -c "from src.dashboard.queries.search import search_entities; print('OK')"` and `python -c "from src.dashboard.utils.tiers import calculate_value_tier, TIER_COLORS; print(calculate_value_tier(0, 0, 0), TIER_COLORS)"` — both should import without errors. Verify tier logic: calculate_value_tier(0, 0, 0) == "HIGH", calculate_value_tier(2, 500_000, 0) == "MEDIUM", calculate_value_tier(5, 50_000, 0) == "LOW".
  </verify>
  <done>search_entities function returns cross-entity results via UNION ALL with parameterized ILIKE. calculate_value_tier correctly classifies 3 tiers. TIER_COLORS provides display colors.</done>
</task>

<task type="auto">
  <name>Task 2: Create lead profile query functions</name>
  <files>src/dashboard/queries/lead_profile.py</files>
  <action>
Create `src/dashboard/queries/lead_profile.py` with these functions, all using `run_query()` from config.py and decorated with `@st.cache_data(ttl="5m")`:

1. `get_lead_overview(cnpj: str) -> pd.DataFrame`:
   - Query proponentes table for single CNPJ: all fields (nome, cnpj, email, telefone, endereco, bairro, municipio, estado, cep, natureza_juridica, total_propostas, total_emendas, valor_total_emendas, total_convenios, valor_total_desembolsos, is_existing_client).
   - Also compute tier_classification using `calculate_value_tier()` from utils.tiers after fetching the row (Python-side, not SQL — keeps SQL simple).
   - Return single-row DataFrame with tier_classification column added.

2. `get_lead_emendas(cnpj: str, limit: int = 100) -> pd.DataFrame`:
   - Reuse the exact query pattern from `qualificacao.py:get_proponente_emendas` but add LIMIT :limit clause.
   - Returns: numero_emenda, parlamentar, valor_emenda, tipo_emenda, apoiador_nome, ministerio.

3. `get_lead_propostas(cnpj: str, limit: int = 100) -> pd.DataFrame`:
   - Reuse the exact query pattern from `qualificacao.py:get_proponente_propostas` but add LIMIT :limit.
   - Returns: transfer_gov_id, titulo, situacao, valor_global, valor_repasse, data_publicacao, programa_nome.

4. `get_lead_ministerios(cnpj: str) -> pd.DataFrame`:
   - Query DISTINCT ministerio/orgao names associated with the proponente's propostas via apoiadores.
   - Use the same JOIN pattern as `_get_ministerios_batch` in qualificacao.py but for a single CNPJ.
   - Returns: orgao, count (number of propostas per orgao).

5. `get_lead_programas(cnpj: str) -> pd.DataFrame`:
   - Query DISTINCT programas associated with the proponente's propostas.
   - JOIN propostas ON proponente_cnpj, then JOIN programas ON programa_id.
   - Returns: programa_nome, programa_id, count (number of propostas per programa).

All queries use parameterized `:cnpj` and `:limit` params (no string interpolation). Follow the established `run_query(query_str, params)` pattern.
  </action>
  <verify>
Run `python -c "from src.dashboard.queries.lead_profile import get_lead_overview, get_lead_emendas, get_lead_propostas, get_lead_ministerios, get_lead_programas; print('All imports OK')"` — should import without errors. Verify function signatures accept expected parameters.
  </verify>
  <done>Five lead profile query functions created, all using run_query() with parameterized SQL, cached with 5m TTL, and returning DataFrames with documented column schemas.</done>
</task>

</tasks>

<verification>
1. All three new modules import cleanly: `from src.dashboard.queries.search import search_entities`, `from src.dashboard.queries.lead_profile import get_lead_overview`, `from src.dashboard.utils.tiers import calculate_value_tier`
2. Tier logic is correct: virgin proponents (0 propostas) = HIGH, low competition with moderate value = MEDIUM, high competition = LOW
3. Search query uses UNION ALL with parameterized ILIKE (no SQL injection)
4. All queries use `run_query()` from config.py (established pattern)
</verification>

<success_criteria>
- Cross-entity search function ready for global search component
- Lead profile queries ready for lead profile page
- Tier classification centralized in single utility
- All functions follow existing codebase patterns (caching, run_query, parameterized SQL)
</success_criteria>

<output>
After completion, create `.planning/phases/08-lead-profile-enhanced-navigation/08-01-SUMMARY.md`
</output>
