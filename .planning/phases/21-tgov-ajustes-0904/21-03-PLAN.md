---
phase: 21-tgov-ajustes-0904
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - web/src/lib/tgov.ts
  - web/src/app/api/tgov/execucao/route.ts
  - web/src/app/tgov/TGovDashboardClient.tsx
autonomous: true
requirements:
  - TAB-SPLIT-EXECUCAO-PC
  - RBAC-TAB-ISOLATION

must_haves:
  truths:
    - "Two separate tabs exist in the TGov dashboard: 'Execução' and 'Prestação de Contas'"
    - "The Execução tab shows only records with situacao ILIKE 'Em Execução'"
    - "The Prestação de Contas tab shows only records with situacao ILIKE '%Prestação de Contas%'"
    - "Each tab has its own independent filters, table, and pagination (resetting one does not affect the other)"
    - "Execution roles (coord_execucao, assistente_execucao, projetista_execucao) see Execução + Prestação de Contas but NOT Aprovação"
    - "Aprovação roles (coord_aprovacao, assistente_aprovacao, projetista) see ONLY Aprovação tab"
    - "General roles (gestor, admin, csm, adm_produto) see all three tabs"
  artifacts:
    - path: "web/src/lib/tgov.ts"
      provides: "TGovTab type with prestacao_contas value"
      contains: "prestacao_contas"
    - path: "web/src/app/api/tgov/execucao/route.ts"
      provides: "mode query param handling for execucao vs prestacao_contas filter"
      contains: "prestacao_contas"
    - path: "web/src/app/tgov/TGovDashboardClient.tsx"
      provides: "Three-tab UI with RBAC filter and per-tab state"
      contains: "prestacao_contas"
  key_links:
    - from: "TGovDashboardClient tab click"
      to: "/api/tgov/execucao?mode=prestacao_contas"
      via: "fetch with mode param"
      pattern: "mode.*prestacao_contas"
    - from: "execucao/route.ts mode param"
      to: "mainConditions"
      via: "ILIKE filter"
      pattern: "ILIKE.*Presta"
---

<objective>
Split the Execução tab into two independent tabs: "Execução" (exact match `ILIKE 'Em Execução'`) and "Prestação de Contas" (ILIKE `'%Prestação de Contas%'`). Update the TGovTab type, execução API to handle `mode` query param, and TGovDashboardClient to render three tabs with RBAC-aware visibility.

Purpose: Execução and Prestação de Contas are distinct phases; users need separate views with independent filters/pagination.
Output: Updated tgov.ts (type), execucao API (mode filter), TGovDashboardClient (tab UI + state)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tgov-ajustes-0904/21-CONTEXT.md
@.planning/phases/21-tgov-ajustes-0904/21-RESEARCH.md
@.planning/phases/21-tgov-ajustes-0904/21-01-SUMMARY.md
@web/src/lib/tgov.ts
@web/src/app/api/tgov/execucao/route.ts
@web/src/app/tgov/TGovDashboardClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TGovTab type update + execucao API mode filter</name>
  <files>
    web/src/lib/tgov.ts
    web/src/app/api/tgov/execucao/route.ts
  </files>
  <action>
**1a. Update `web/src/lib/tgov.ts`:**

Change `TGovTab` type from:
```typescript
export type TGovTab = 'aprovacao' | 'execucao'
```
to:
```typescript
export type TGovTab = 'aprovacao' | 'execucao' | 'prestacao_contas'
```

The `DEFAULT_TGOV_TAB` constant stays `'aprovacao'` — no change needed.

**1b. Update `web/src/app/api/tgov/execucao/route.ts`:**

In the `GET` handler, after parsing `searchParams` (around line 198-210), add `mode` param parsing:
```typescript
const mode = searchParams.get('mode') ?? 'execucao' // 'execucao' | 'prestacao_contas'
```

Then, in the `mainConditions` section — BEFORE the `ano` filter and user-level filters — add the situacao filter based on mode. Insert immediately after `const mainConditions: string[] = []`:
```typescript
// Split filter: execucao vs prestacao_contas mode
if (mode === 'execucao') {
  mainConditions.push(`LOWER(pe.situacao) = 'em execução'`)
} else if (mode === 'prestacao_contas') {
  mainConditions.push(`pe.situacao ILIKE '%Prestação de Contas%'`)
}
// If neither (shouldn't happen), no mode filter applied — shows all execução data
```

Note: Using `LOWER(pe.situacao) = 'em execução'` for execucao mode to handle case variants ('Em Execução' and 'Em execução' per Pitfall 8 in research). Using `ILIKE` for PC to catch all PC variants.

Also verify the existing `status` user filter (around line 227-230) does not conflict — it should remain as-is since it's an additional user filter on top of the mode filter. This is correct behavior (user can further filter within the tab).

Also add projetista_execucao to the tecnico_id filter block (if not already done in Plan 01 Task 2). Find the projetista filter (search for `session.role === 'projetista'` in this file) and ensure it reads:
```typescript
if (session.role === 'projetista' || session.role === 'projetista_execucao') {
  params.push(session.userId)
  mainConditions.push(`pe.tecnico_id = $${params.length}::uuid`)
}
```
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>
      Test: curl "$API/api/tgov/execucao?mode=execucao&page=1&page_size=5" — results should only have situacao matching 'Em Execução'
      Test: curl "$API/api/tgov/execucao?mode=prestacao_contas&page=1&page_size=5" — results should only have situacao containing 'Prestação de Contas'
    </manual>
  </verify>
  <done>
    - TGovTab type includes 'prestacao_contas'
    - execucao/route.ts parses mode param and adds appropriate mainConditions filter
    - LOWER() used for Em Execução to handle case variants
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: TGovDashboardClient — three tabs + RBAC filter + per-tab state</name>
  <files>web/src/app/tgov/TGovDashboardClient.tsx</files>
  <action>
Read TGovDashboardClient.tsx carefully before making changes. The file is large (~2200+ lines). Key locations:

- Tab render section (line ~524): `(['aprovacao', 'execucao'] as TGovTab[]).filter(...).map(...)`
- handleTabSwitch function (search for `function handleTabSwitch` or `handleTabSwitch`)
- API fetch (search for `/api/tgov/${tab}`)
- Tab content render (search for `activeTab === 'execucao'`)

**Change 1: Import APROVACAO_ONLY_ROLES and EXECUCAO_ONLY_ROLES**

These constants were added to tgov.ts in Plan 01. Add them to the existing import from `@/lib/tgov`:
```typescript
import {
  // ...existing imports...
  APROVACAO_ONLY_ROLES,
  EXECUCAO_ONLY_ROLES,
} from '@/lib/tgov'
```

**Change 2: Replace the tab filter inline JSX (line ~524)**

Replace the current filter:
```typescript
{(['aprovacao', 'execucao'] as TGovTab[])
  .filter(tab => !((userRole === 'coord_aprovacao' || userRole === 'projetista' || userRole === 'assistente_aprovacao') && tab === 'execucao'))
  .map((tab) => (
    <button key={tab} onClick={() => handleTabSwitch(tab)} ...>
      {tab === 'aprovacao' ? 'Aprovação' : 'Execução'}
    </button>
  ))}
```

With the new three-tab filter:
```typescript
{(['aprovacao', 'execucao', 'prestacao_contas'] as TGovTab[])
  .filter(tab => {
    const isAprovacaoRole = (APROVACAO_ONLY_ROLES as readonly string[]).includes(userRole)
    const isExecucaoRole = (EXECUCAO_ONLY_ROLES as readonly string[]).includes(userRole)
    if (isAprovacaoRole && tab !== 'aprovacao') return false
    if (isExecucaoRole && tab === 'aprovacao') return false
    return true
  })
  .map((tab) => (
    <button
      key={tab}
      onClick={() => handleTabSwitch(tab)}
      className={`px-4 py-1.5 rounded-lg text-sm font-medium transition-all ${
        activeTab === tab
          ? 'bg-white text-gray-900 shadow-sm'
          : 'text-gray-500 hover:text-gray-700'
      }`}
    >
      {tab === 'aprovacao' ? 'Aprovação' : tab === 'execucao' ? 'Execução' : 'Prestação de Contas'}
    </button>
  ))}
```

**Change 3: Update handleTabSwitch to handle prestacao_contas**

Find `handleTabSwitch` function. Update the `setMainFilters` call to treat `prestacao_contas` the same as `execucao`:
```typescript
function handleTabSwitch(tab: TGovTab) {
  setActiveTab(tab)
  setTableFilters(DEFAULT_TABLE_FILTERS)
  setMainFilters(tab === 'execucao' || tab === 'prestacao_contas'
    ? DEFAULT_EXECUCAO_MAIN_FILTERS
    : DEFAULT_MAIN_FILTERS)
  setPage(1)
  setSelectedExecRow(null)
  setSelectedAprovRow(null)
}
```

**Change 4: Update the API fetch to pass mode param**

Find the `fetchData` function or `useEffect` that does `fetch('/api/tgov/${tab}')`. Update to handle `prestacao_contas`:

```typescript
// Map tab to API path and mode param
const apiPath = activeTab === 'prestacao_contas' ? 'execucao' : activeTab
const modeParam = activeTab === 'prestacao_contas' ? '&mode=prestacao_contas'
                : activeTab === 'execucao' ? '&mode=execucao'
                : ''
const res = await fetch(`/api/tgov/${apiPath}?${params.toString()}${modeParam}`)
```

Locate the exact fetch call and replace accordingly. Do NOT change the pattern for aprovacao — it fetches from `/api/tgov/aprovacao` with no mode param.

**Change 5: Update the tab content conditional render**

Find the section that renders `{activeTab === 'execucao' ? <ExecucaoTable ... /> : <AprovacaoTable ... />}`. Update to a three-way conditional:

```typescript
{activeTab === 'prestacao_contas' ? (
  <ExecucaoTable
    rows={execucaoRows}
    // ... same props as the execucao tab
    userRole={userRole}
    // ... all other existing ExecucaoTable props
  />
) : activeTab === 'execucao' ? (
  <ExecucaoTable
    rows={execucaoRows}
    // ... same existing props
  />
) : (
  <AprovacaoTable
    rows={aprovacaoRows}
    // ... existing props
  />
)}
```

Both execucao and prestacao_contas tabs reuse `ExecucaoTable` with the same props — the API filtering handles the data difference.

**Change 6: Compute initial tab based on role**

Find where `DEFAULT_TGOV_TAB` is used as the initial `useState` value for `activeTab`. Update to compute the initial tab dynamically:

```typescript
// Execution roles should start on execucao tab
const initialTab: TGovTab = (EXECUCAO_ONLY_ROLES as readonly string[]).includes(userRole)
  ? 'execucao'
  : DEFAULT_TGOV_TAB

const [activeTab, setActiveTab] = useState<TGovTab>(initialTab)
```
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/projetustgov/web && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>
      1. Log in as gestor → verify 3 tabs visible: Aprovação, Execução, Prestação de Contas
      2. Click Prestação de Contas → verify table only shows records with PC situações
      3. Click Execução → verify table only shows records with 'Em Execução' situação
      4. Filter reset: switching tabs should reset filters independently
    </manual>
  </verify>
  <done>
    - Three tabs render for gestor/admin/adm_produto/csm
    - APROVACAO_ONLY_ROLES see only Aprovação tab
    - EXECUCAO_ONLY_ROLES see only Execução + Prestação de Contas tabs
    - Prestação de Contas tab fetches /api/tgov/execucao?mode=prestacao_contas
    - Execution roles start on 'execucao' tab by default
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- TGovTab type in tgov.ts includes 'prestacao_contas'
- execucao/route.ts has mode param handling with ILIKE filters
- TGovDashboardClient.tsx tab filter uses APROVACAO_ONLY_ROLES and EXECUCAO_ONLY_ROLES constants
- Three tab buttons render in the UI for general roles
</verification>

<success_criteria>
- Prestação de Contas tab exists as a fully independent tab with correct data filtering
- RBAC tab isolation: aprovacao roles see only Aprovação; execucao roles see only Execução+PC
- Switching between tabs resets filters independently
- No regression in existing Aprovação or Execução tab behavior
</success_criteria>

<output>
After completion, create `.planning/phases/21-tgov-ajustes-0904/21-03-SUMMARY.md`
</output>
